REM Debug TAN function issue
angle = 0.5
PRINT "angle = ";
PRINT angle

PRINT "TAN(angle) = ";
PRINT TAN(angle)

PRINT "TAN(0.5) = ";
PRINT TAN(0.5)

PRINT "SIN(0.5) = ";
PRINT SIN(0.5)

PRINT "COS(0.5) = ";
PRINT COS(0.5)

sinVal = SIN(0.5)
cosVal = COS(0.5)
ratio = sinVal / cosVal
PRINT "SIN(0.5) / COS(0.5) = ";
PRINT ratio

REM Test with different angles
PRINT "TAN(1.0) = ";
PRINT TAN(1.0)

PRINT "TAN(0) = ";
PRINT TAN(0)

PRINT "TAN(1) = ";
PRINT TAN(1)
