// BUG-015 regression: Viper.Text.Fmt.BoolYN should return lowercase "yes"/"no"
module TestBugfixBoolYN;

bind Viper.Terminal;

func start() {
    var yes = Viper.Text.Fmt.YesNo(true);
    var no = Viper.Text.Fmt.YesNo(false);
    if (yes == "yes" && no == "no") {
        Say("RESULT: ok");
    } else {
        Say("FAIL: expected yes/no, got:");
        Say(yes);
        Say(no);
    }
}
