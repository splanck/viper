// BUG-015 regression: Zanna.Text.Fmt.BoolYN should return lowercase "yes"/"no"
module TestBugfixBoolYN;

bind Zanna.Terminal;

func start() {
    var yes = Zanna.Text.Fmt.YesNo(true);
    var no = Zanna.Text.Fmt.YesNo(false);
    if (yes == "yes" && no == "no") {
        Say("RESULT: ok");
    } else {
        Say("FAIL: expected yes/no, got:");
        Say(yes);
        Say(no);
    }
}
