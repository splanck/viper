module test_game3d_destroyed_handle_reject;

bind Viper.Game3D as Game3D;

func start() {
    var world = Game3D.World3D.New("Game3D Destroyed Handle Reject", 32, 24);
    world.Destroy();
    world.Frame;
}
