module RuntimeTest04;

bind "./_support";

// EXPECT_OUT: RESULT: ok
// EXPECT_ARGS: alpha beta
// COVER: Zanna.System.Environment.GetArgumentCount
// COVER: Zanna.System.Environment.GetArgument
// COVER: Zanna.System.Environment.GetCommandLine

func start() {    var count = Zanna.System.Environment.GetArgumentCount();
    assertTrue(count >= 1, "argc");
    if (count >= 3) {
        assertEqStr(Zanna.System.Environment.GetArgument(1), "alpha", "arg1");
        assertEqStr(Zanna.System.Environment.GetArgument(2), "beta", "arg2");
    }
    var cmd = Zanna.System.Environment.GetCommandLine();
    assertNotEmpty(cmd, "cmdline");
    report();
}
