module RuntimeTest17;

bind "./_support";

// EXPECT_OUT: RESULT: ok
// EXPECT_ARGS: gamma delta

func start() {    var argc = Zanna.System.Environment.GetArgumentCount();
    assertTrue(argc >= 1, "argc");
    if (argc >= 3) {
        assertEqStr(Zanna.System.Environment.GetArgument(1), "gamma", "arg1");
        assertEqStr(Zanna.System.Environment.GetArgument(2), "delta", "arg2");
    }
    var cmd = Zanna.System.Environment.GetCommandLine();
    assertNotEmpty(cmd, "cmd");

    Zanna.Time.Clock.Sleep(1);
    Zanna.Time.Clock.Sleep(1);

    report();
}
