module RuntimeTest35;

bind Zanna.Terminal;

func requireValue(x: Integer?) -> Integer {    if (x == null) {
        return -1;
    }

    var y: Integer = x;
    Zanna.Terminal.SayInt(x);
    return y;
}

func start() {    var direct = requireValue(42);
    if (direct != 42) {
        Zanna.Terminal.Say("FAIL: guard_narrowing");
        return;
    }

    var maybe: Integer? = 7;
    if (maybe != null) {
        var branchValue: Integer = maybe;
        if (branchValue != 7) {
            Zanna.Terminal.Say("FAIL: branch_assignment");
            return;
        }
        Zanna.Terminal.SayInt(maybe);
    } else {
        Zanna.Terminal.Say("FAIL: branch_narrowing");
        return;
    }

    Zanna.Terminal.Say("RESULT: ok");
}
