module TestMatchSimple;

// Test: Simple match statement

func start() {
    Zanna.Terminal.Say("=== Simple Match Test ===");

    var x: Integer = 2;
    var result: Integer = 0;

    match x {
        0 => { result = 10; }
        1 => { result = 20; }
        2 => { result = 30; }
        _ => { result = 99; }
    }

    Zanna.Terminal.Say("Result: " + Zanna.String.FromInt(result));

    if (result == 30) {
        Zanna.Terminal.Say("RESULT: ok");
    } else {
        Zanna.Terminal.Say("RESULT: fail");
    }
}
