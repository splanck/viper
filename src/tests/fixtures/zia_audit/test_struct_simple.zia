module TestValueSimple;

// Test: Simple value type

struct Point2D {
    expose Number x;
    expose Number y;

    expose func init(px: Number, py: Number) {
        x = px;
        y = py;
    }
}

func start() {
    Zanna.Terminal.Say("=== Value Type Simple Test ===");

    var p1: Point2D = new Point2D(3.0, 4.0);
    Zanna.Terminal.Say("Created point");

    // Test field access
    if (p1.x > 2.0 && p1.y > 3.0) {
        Zanna.Terminal.Say("Fields look correct");
    }

    Zanna.Terminal.Say("RESULT: ok");
}
