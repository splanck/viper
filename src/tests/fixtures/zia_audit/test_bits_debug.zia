module TestBitsDebug;

func start() {
    Zanna.Terminal.Say("Testing Bits.Not and Bits.Flip:");

    var not0: Integer = Zanna.Math.Bits.Not(0);
    Zanna.Terminal.Say("Bits.Not(0) = " + Zanna.String.FromInt(not0));

    var flip0: Integer = Zanna.Math.Bits.Flip(0);
    Zanna.Terminal.Say("Bits.Flip(0) = " + Zanna.String.FromInt(flip0));

    var langNot0: Integer = ~0;
    Zanna.Terminal.Say("~0 = " + Zanna.String.FromInt(langNot0));

    var not1: Integer = Zanna.Math.Bits.Not(1);
    Zanna.Terminal.Say("Bits.Not(1) = " + Zanna.String.FromInt(not1));

    var flip1: Integer = Zanna.Math.Bits.Flip(1);
    Zanna.Terminal.Say("Bits.Flip(1) = " + Zanna.String.FromInt(flip1));
}
