// Minimal test for text rendering
module main;

bind Viper.Graphics;
bind Viper.Graphics.Color;
bind Viper.Time;

func main() {
    var canvas = new Canvas("Text Test", 640, 480);

    var white = RGB(255, 255, 255);
    var running = 1;

    while running == 1 {
        canvas.Poll();

        if canvas.ShouldClose != 0 {
            running = 0;
        }

        canvas.Clear(0);
        canvas.Text(100, 100, "HELLO WORLD", white);
        canvas.Text(100, 120, "Test 123", white);
        canvas.Flip();

        Viper.Time.SleepMs(16);
    }
}
