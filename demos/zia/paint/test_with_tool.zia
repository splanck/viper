module test_with_tool;

bind "./config";
bind "./canvas";
bind "./colors";
bind "./brush";
bind "./tools/pencil";

bind Viper.Graphics;

func main() {
    // Create graphics canvas
    var gfx = Canvas.New(config.WINDOW_TITLE, 800, 600);

    // Create DrawingCanvas and initialize with size
    var c = new canvas.DrawingCanvas();
    c.initWithSize(640, 480);

    // Create ColorManager
    var clr = new colors.ColorManager();

    // Create BrushSettings
    var brsh = new brush.BrushSettings();

    // Create PencilTool
    var tool = new pencil.PencilTool();
}
