module gfx_test;

// Very minimal graphics test - just create and destroy a canvas
func main() {
    var canvas = Viper.Graphics.Canvas.New("Test", 100, 100);
    // Just create and immediately exit
}
