module CancelTokenDemo;
bind Viper.Terminal;
func start() {
    Say("=== Viper.Threads.CancelToken Demo ===");
    var ct = new Viper.Threads.CancelToken();
    SayBool(ct.IsCancelled);
    ct.Cancel();
    SayBool(ct.IsCancelled);
    ct.Reset();
    SayBool(ct.IsCancelled);
    Say("done");
}
