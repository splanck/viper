module SafeI64Demo;
bind Viper.Terminal;
func start() {
    Say("=== Viper.Threads.SafeI64 Demo ===");
    var a = new Viper.Threads.SafeI64(0);
    SayInt(a.Get());
    a.Set(42);
    SayInt(a.Get());
    SayInt(a.Add(8));
    SayInt(a.Get());
    Say("done");
}
