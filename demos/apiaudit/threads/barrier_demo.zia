module BarrierDemo;
bind Viper.Terminal;
func start() {
    Say("=== Viper.Threads.Barrier Demo ===");
    var b = new Viper.Threads.Barrier(3);
    SayInt(b.Parties);
    SayInt(b.Waiting);
    b.Reset();
    SayInt(b.Waiting);
    Say("done");
}
