module StreamDemo;
bind Viper.Terminal;
func start() {
    Say("=== Viper.IO.Stream Demo ===");
    var s = Viper.IO.Stream.OpenMemory();
    SayInt(s.Pos);
    SayInt(s.Len);
    s.WriteByte(65);
    s.WriteByte(66);
    SayInt(s.Len);
    s.Close();
    Say("done");
}
