PRINT "=== API Audit: Viper.Graphics.SpriteSheet ==="
PRINT "NOTE: Graphics classes require a canvas/window. API surface test only."
PRINT "Constructor: New(texWidth, texHeight)"
PRINT "Properties: get_RegionCount, get_Width, get_Height"
PRINT "Methods: AddRegion(x, y, w, h), GetRegion(idx)"
PRINT "=== SpriteSheet Audit Complete (API surface only) ==="
END
