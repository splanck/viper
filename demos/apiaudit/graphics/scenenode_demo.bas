PRINT "=== API Audit: Viper.Graphics.SceneNode ==="
PRINT "NOTE: Graphics classes require a canvas/window. API surface test only."
PRINT "Constructor: New(name)"
PRINT "Properties: get_X, get_Y, get_Visible, get_Tag"
PRINT "Methods: AddChild(node), RemoveChild(node)"
PRINT "Methods: SetPosition(x, y), SetScale(sx, sy), SetRotation(angle)"
PRINT "Methods: SetVisible(v), SetTag(tag)"
PRINT "=== SceneNode Audit Complete (API surface only) ==="
END
