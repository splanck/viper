module LazyDemo;
bind Viper.Terminal;
func start() {
    Say("=== Viper.Lazy Demo ===");
    var lz = Viper.Lazy.OfI64(42);
    SayBool(lz.IsEvaluated);
    SayInt(lz.GetI64());
    SayBool(lz.IsEvaluated);
    var ls = Viper.Lazy.OfStr("hello");
    Say(ls.GetStr());
    Say("done");
}
