module LazySeqDemo;
bind Viper.Terminal;
func start() {
    Say("=== Viper.LazySeq Demo ===");
    var seq = Viper.LazySeq.Range(1, 10, 1);
    SayInt(seq.Index);
    SayBool(seq.IsExhausted);
    SayInt(seq.Count());
    seq.Reset();
    SayInt(seq.Index);
    Say("done");
}
