module QuatDemo;
bind Viper.Terminal;
func start() {
    Say("=== Viper.Math.Quat Demo ===");
    var q = new Viper.Math.Quat(0.0, 0.0, 0.0, 1.0);
    SayNum(q.X);
    SayNum(q.Y);
    SayNum(q.Z);
    SayNum(q.W);
    SayNum(q.Len());
    SayNum(q.LenSq());
    Say("done");
}
