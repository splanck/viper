module StackDemo;
bind Viper.Terminal;
func start() {
    Say("=== Viper.Collections.Stack Demo ===");
    var s = new Viper.Collections.Stack();
    s.Push("a"); s.Push("b"); s.Push("c");
    SayInt(s.Len);
    SayBool(s.IsEmpty);
    s.Clear();
    SayBool(s.IsEmpty);
    Say("done");
}
