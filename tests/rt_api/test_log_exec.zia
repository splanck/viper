// test_log_exec.zia — Log, Exec, NumberFormat
module TestLogExec;

bind Viper.Terminal;
bind Viper.Diagnostics.Log;
bind Viper.System.Exec;
bind Viper.Text.NumberFormat;

func start() {
    SayInt(Viper.Diagnostics.Log.DEBUG);
    SayInt(Viper.Diagnostics.Log.INFO);
    SayInt(Viper.Diagnostics.Log.WARN);
    SayInt(Viper.Diagnostics.Log.ERROR);
    SayInt(Viper.Diagnostics.Log.OFF);
    SayInt(Viper.Diagnostics.Log.Level);
    SayBool(Viper.Diagnostics.Log.Enabled(Viper.Diagnostics.Log.DEBUG));
    Viper.Diagnostics.Log.Info("test info message");
    Viper.Diagnostics.Log.Warn("test warn message");
    Viper.Diagnostics.Log.Error("test error message");

    Say(ShellCapture("echo hello"));

    Say(Bytes(1048576));
    Say(Ordinal(1));
    Say(Ordinal(2));
    Say(Ordinal(3));
    Say(Ordinal(11));
    Say(Pad(42, 5));
    Say(Percent(0.756));
    Say(Thousands(1234567, ","));
    Say(ToWords(42));
    Say(Decimals(3.14159, 2));
    Say(Currency(1234.56, "$"));

    Say("done");
}
