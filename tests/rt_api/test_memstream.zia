// test_memstream.zia — IO.MemStream, IO.BinFile, IO.LineReader, IO.LineWriter, IO.Compress
module TestMemStream;

bind Viper.Terminal;

func start() {
    // --- MemStream: integer read/write ---
    var ms = new Viper.IO.MemStream();
    SayInt(ms.Pos);
    SayInt(ms.Length);

    ms.WriteI8(42);
    ms.WriteI16(1000);
    ms.WriteI32(100000);
    ms.WriteI64(9999999999);
    SayInt(ms.Length);

    ms.Seek(0);
    SayInt(ms.ReadI8());
    SayInt(ms.ReadI16());
    SayInt(ms.ReadI32());
    SayInt(ms.ReadI64());

    // --- MemStream: string read/write ---
    ms.Clear();
    ms.WriteStr("hello world");
    SayInt(ms.Length);
    ms.Seek(0);
    Say(ms.ReadStr(11));

    // --- MemStream: unsigned types ---
    ms.Clear();
    ms.WriteU8(200);
    ms.WriteU16(60000);
    ms.WriteU32(3000000000);
    ms.Seek(0);
    SayInt(ms.ReadU8());
    SayInt(ms.ReadU16());
    SayInt(ms.ReadU32());

    // --- MemStream: capacity and skip ---
    SayInt(ms.Capacity);
    ms.Clear();
    ms.WriteI8(1);
    ms.WriteI8(2);
    ms.WriteI8(3);
    ms.Seek(0);
    ms.Skip(1);
    SayInt(ms.Pos);

    // --- Text streams via runtime constructors ---
    var textPath = "/tmp/viper_rt_api_memstream_lines.txt";
    var writer = new Viper.IO.LineWriter(textPath);
    writer.Write("alpha");
    writer.WriteLn("beta");
    writer.Close();

    var reader = new Viper.IO.LineReader(textPath);
    SayInt(reader.PeekChar());
    Say(reader.Read());
    Say(reader.Read());
    SayBool(reader.Eof);
    reader.Close();

    // --- Binary file via runtime constructor ---
    var binPath = "/tmp/viper_rt_api_memstream.bin";
    var wf = new Viper.IO.BinFile(binPath, "w");
    wf.WriteByte(65);
    wf.WriteByte(66);
    wf.Close();

    var rf = new Viper.IO.BinFile(binPath, "r");
    SayInt(rf.ReadByte());
    SayInt(rf.ReadByte());
    SayBool(rf.Eof);
    rf.Close();

    // --- Compress ---
    var compressed = Viper.IO.Compress.DeflateStr("hello world hello world hello world");
    Say("compressed ok");
    var decompressed = Viper.IO.Compress.InflateStr(compressed);
    Say(decompressed);

    var gzipped = Viper.IO.Compress.GzipStr("test data for gzip");
    var gunzipped = Viper.IO.Compress.GunzipStr(gzipped);
    Say(gunzipped);

    Say("done");
}
