module TestEnvMachine;
bind Viper.System.Environment;
bind Viper.System.Machine;
bind Viper.Terminal;

func start() {
    Say(Platform());
    Say(PlatformVersion());
    Say(UserName());
    Say(HomeDir());
    SayInt(Viper.System.Environment.CpuCount());
    SayInt(Viper.System.Machine.CpuCount());
    SayInt(PageSize());
    Say(Arch());
    Say(OsName());
    Say(Endian());
    SayInt(PointerSize());
    Say(Cwd());
    Say("done");
}
