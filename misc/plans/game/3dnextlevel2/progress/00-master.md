# Master progress

Tracks global readiness for the 3D scale tier. Detailed work items live in the
sibling files. All rows start `todo`; this is a fresh plan.

**Status legend (graded against this macOS host).** `done` = everything closeable
in this environment is complete and proven; only formally **waived** scope may
remain. `partial` = the macOS/CPU core has landed but *genuine closeable-here work*
or a *required recorded baseline* is still open — the **Remaining/Notes** column
states exactly what. `todo` = not started. Most graphics rows stay `partial` on a
mix of (a) waived scope this host can't produce — cross-platform Windows/Linux runs
and GPU-backend upload on D3D11/OpenGL (W2-002/003), Basis/Draco/meshopt (W2-004),
VM callback sugar (W2-001) — and (b) real remaining work: Metal GPU compressed
upload (capability bits are still `false`), ETC2/ASTC decode, per-API docs+ctest,
recorded perf baselines. A `partial` row with a large
*Implemented* column is core-complete with that tail still open, not barely-started.

## Overall gates

| ID | Gate | Source | Status | Proof / link | Notes |
|---|---|---|---|---|---|
| GATE-001 | Carryover (Phase C) closed before broad new work | `carryover.md`, `roadmap.md` Phase C | done | Local macOS CO-1/CO-11 evidence is green; CO-2 is re-waived; CO-3..CO-12 local-closeable rows are done | Done on this host; only Windows/Linux CO-1/CO-11 host evidence remains (waived here) |
| GATE-002 | Determinism preserved: `runFrames` parity with job pool on/off, VM/native | `runtime-changes.md` §1, `roadmap.md` Phase 0/1 | done | `g3d_3dnext2_surface_probe`; `test_rt_game3d` worker-count replay parity | Core Principle #5; determinism proven, only the waived VM callback-loop sugar (W2-001) remains |
| GATE-003 | Every phase green on macOS/Windows/Linux (`-L graphics3d`) | `roadmap.md` Testing | todo |  | Core Principle #7; CO-1 first |
| GATE-004 | Performance harness records before/after for every scale feature | `roadmap.md` Phase 0 | done | Release macOS open-world perf baseline; `g3d_openworld_slice_perf_harness`; `g3d_3dnext2_surface_probe`; `test_rt_scene3d` 10k fixture | Harness records on this host; only Windows/Linux perf lanes remain (waived here) |
| GATE-005 | Software-backend correctness baseline for each new visual feature | `README.md` §6 | todo |  | GPU parity capability-gated + smoke |
| GATE-006 | All systems opt-in; bounded-scene path byte-unchanged with flags off | `README.md` §6 | partial | `test_rt_game3d` bounded floating-origin toggle-off byte-equality proof | Broader named bounded-scene game/sample no-regression remains NL3-032 |
| GATE-007 | Zero new dependencies; from-scratch implementations | `README.md` §5 | todo |  | Core Principle #6 |
| GATE-008 | `check_runtime_completeness.sh` green + docs+ctest per new API | `runtime-changes.md` Validation | partial | `check_runtime_completeness.sh` green; `test_runtime_surface_cli` strict audit now green after classifying previously unclassified internal helper symbols (`RuntimeSurfacePolicy.inc`: commit-queue header + glTF scene/camera, IK, blend-tree, terrain helpers) | Per-new-API docs+ctest coverage still incomplete across phases |
| GATE-009 | Any IL/VM-touching change has an ADR | `README.md` §5 | todo |  | Core Principle #1 |
| GATE-010 | Public API names match current `Viper.*` runtime conventions and class-id ABI | `review.md`, `api-spec.md` | done | `test_graphics3d_abi_surface`; `ctest --test-dir build -R '^(test_graphics3d_abi_surface|test_rt_game3d)$' --output-on-failure` | fully qualified classes, `3D` suffixes, Graphics3D PascalCase, Game3D lower/camel where existing style requires it; appended class ids |

## Phase status

| ID | Phase | Status | Entry dependency | Exit proof / link | Notes |
|---|---|---|---|---|---|
| PHASE-C | Carryover: close out 3D Next Level | done | Prior plan ~90% done | Local macOS graphics3d and Release perf/GPU lanes green; local-closeable CO rows closed or re-waived | Done on this host; only Windows/Linux CO-1/CO-11 host evidence remains (waived here) |
| PHASE-0 | Foundations spike (determinism/threading/index/perf) | done | PHASE-C prerequisites | `02-decisions.md` closed; `g3d_3dnext2_surface_probe`; `g3d_openworld_slice_perf_harness`; Release macOS perf baseline | Done on this host; only cross-platform perf host runs remain (waived here) |
| PHASE-1 | Concurrency runtime (job system) | done | PHASE-0 decisions | `g3d_3dnext2_surface_probe`; `test_rt_g3d_commit_queue`; `test_rt_game3d`; `test_rt_parallel`; `test_rt_parallel_reduce`; `scripts/g3d_tsan_concurrency_lane.sh` | Worker substrate, ordered merge proof, Game3D worker controls, internal main-thread commit queue, asset-worker lane coverage, and focused TSan race-stress evidence are closed locally; formal VM/native determinism acceptance remains tracked by NL3-031 |
| PHASE-2 | Floating origin | partial | PHASE-0 strategy | `test_rt_scene3d` far-origin query/raycast precision, `test_rt_physics3d` far-origin body integration plus body/contact/query rebase, `test_rt_canvas3d` camera-relative matrix/light/raw-generated upload proof plus sprite rebase, `test_rt_particles3d_contract` particle rebase, plus Scene3D/Game3D camera/body/listener/effect-registry rebase coverage and rendered 50 km parity | Double spatial/query bounds, physics body integration, matrix-backed plus raw/generated camera-relative upload, standalone particle/sprite/physics rebase hooks, cross-system Game3D rebase boundary, rendered 50 km near/far parity, and flag-off byte equality landed; optional per-cell policy and broader bounded-scene sample no-regression remain |
| PHASE-3 | Spatial acceleration index | partial | PHASE-0 choice | `test_rt_scene3d` indexed query/draw parity and 10k generated cull/query fixture | Internal Scene3D index landed; measured perf lane and physics broadphase sharing remain |
| PHASE-4 | Async asset + GPU-upload streaming | partial | PHASE-1 | `test_rt_game3d` AssetHandle3D worker-scheduled/cancel/error + cache residency/background `Preload` / `PreloadAsset` baseline plus corrupt required texture-payload errors, zero-upload-budget pending textured commits, partial decoded-texture upload progress, texture-backed cache-budget eviction, `Assets3D.SetResidencyHint` priority/distance eviction, `Assets3D.GetResidentBytes` resident-byte load/clear churn, and stale-preload clear guarding; `test_rt_gltf` preloaded external-buffer, data URI, worker-decoded PNG/BMP/JPEG/GIF image with sliced RGBA-to-`Pixels` commit preparation, worker-decoded static/skinned/morph-target mesh POD including triangle strips/fans, sparse accessors, JOINTS_1 reduction, skin remapping, morph deltas, node morph weights, and no-normal commit regeneration, bufferView-image, missing-required-buffer, accessor-range preload proof, and corrupt required texture rejection for preload plus blocking load; `test_gif_decode` raw first-frame GIF decoder coverage; `test_rt_g3d_commit_queue` cost-budget drain coverage; `test_rt_canvas3d` + `test_vgfx3d_backend_utils` backend texture upload budget/telemetry and pending-byte return-to-baseline proof; `g3d_openworld_slice_streaming_hitch_probe` records blocking-vs-async timing and zero-upload-budget pending/release behavior; `g3d_openworld_slice_probe` true async completion wait; `test_rt_model3d` glTF parse-failure recovery; `test_runtime_surface_audit` internal preloaded-loader surface | `AssetHandle3D`, glTF/GLB root plus external/embedded buffer/image worker staging, worker-side PNG/BMP/JPEG/GIF-to-RGBA POD image decode plus bounded commit-side `Pixels` preparation, static/skinned/morph-target triangle-list/strip/fan/sparse-accessor mesh decode to `Mesh3D`/`MorphTarget3D` POD with commit-side normal regeneration, skin remapping, and morph attachment, required-buffer/accessor-range/corrupt-required-texture preload validation through the cost-aware commit queue, world-drained background filesystem `Preload` and package-aware `PreloadAsset`, `SetUploadBudget` decoded-image commit-slice gating, `Canvas3D.SetTextureUploadBudget`, `TextureUploadPendingBytes`, and `TextureUploadBytes` backend material/native-compressed upload controls/telemetry, `SetResidencyBudget`/`GetResidentBytes`/`Evict` with decoded material texture bytes counted in template residency, `SetResidencyHint` priority/distance cache eviction, generation-guarded `ClearCache`, malformed/unsupported/corrupt-texture async load-error states, resident-byte churn proof, backend pending-byte baseline proof, and a named software hitch probe landed; the named native-compressed hitch rerun is covered locally |
| PHASE-5 | World partition + terrain streaming | partial | PHASE-1/3/4, PHASE-2 | `test_rt_game3d` WorldStream3D telemetry plus manifest-driven VSCN cell load/unload, measured loaded-cell base/resident-LOD/impostor/material texture residency with mesh/LOD remeasurement, streamed material/collision/nav metadata inspection, rendered heightmapped Terrain3D tile payload residency, static heightfield collider residency, terrain nav-bake source residency, staged `update(dt)` load-budget telemetry, stitched adjacent terrain LOD-seam repair, a 9600-unit / >4 km2 tile proof beyond the single-heightmap cap, and `g3d_openworld_slice_long_traversal` named traversal hitch/memory proof over 18,939,904 m2 | streamed nav bake/export tooling remains |
| PHASE-6 | Visibility scaling (occlusion + auto-LOD/HLOD) | done | PHASE-3 | `test_rt_canvas3d` dense occlusion reduction plus candidate telemetry; `test_rt_scene3d` indexed occlusion-candidate reduction, portal/PVS room culling, and LOD/impostor coverage; `g3d_openworld_slice_visibility_dense_probe` authored city/forest reduction proof | Reconciles CO-8, index-fed CPU occlusion candidates, authored portal/PVS, and the local named reduction baseline; automatic mesh simplification/backend-baked HLOD remains stretch scope |
| PHASE-7 | Lighting scaling (clustered + CSM) | done | PHASE-3; CO-6 | `test_rt_canvas3d` software many-light/CSM capability baseline; `test_rt_canvas3d_gpu_paths` forward fallback, shadow-caster selection, and three-cascade CSM payload/pass proof; `g3d_openworld_slice_gpu_smoke` 24-light GPU clustered proof plus 3-cascade Metal CSM fixture | Local Metal Release baseline recorded |
| PHASE-8 | Physics depth | done | PHASE-3 | Warm-started sequential-impulse solver + sleep islands land a stable deterministic resting box stack (`test_rt_physics3d` `test_box_stack_rests_stably`); also `test_rt_game3d` contact accessors and `test_rt_physics3d` solver iterations, AABB manifolds, rotated-box clipped OBB manifolds, island-batched contact scheduling with `LastSolver*` telemetry, a named 257-body / 32-pile `PHYSICS_ISLAND_BATCH_TARGET`, mesh BVH sphere/capsule/box/convex-hull narrow phase with `PHYSICS_MESH_BVH_TARGET`, GJK/EPA hull/simple slices with `PHYSICS_CONVEX_GJK_TARGET`, sleeping controls, sparse body-count stress, hinge/rope/SixDof slices, and SixDof joint-frame pose-angle limits stable under spin and linear motor drive | Phase 8 NL3 action list complete locally |
| PHASE-9 | Navigation + AI depth | done | PHASE-3/5 | `test_rt_navmesh_blend`: from-scratch voxel autogen baker (`Bake` voxelizes→slope→**agent-radius erosion**→shared-corner grid mesh, decoupled from input density), narrow-corridor erosion proof, and `find_tri` spatial query-grid parity (O(cell)); retained tiled-bake voxel source data so **tile-local `RebuildTile`** refreshes one tile's geometry/heights/blocked state while far-tile rebuilds leave stale source edits untouched; exact triangle-footprint obstacle carving; area/cost metadata with weighted `LastPathCost`; off-mesh link kind/cost/state metadata; `test_rt_navagent3d` reciprocal velocity-obstacle avoidance, head-on deadlock avoidance, multi-pair crowd cross, **O(N) avoidance peer-culling** (spatial agent grid, grid-vs-full-scan parity verified), and `NAVAGENT_CROWD_TARGET`; `g3d_openworld_slice_probe` streamed-terrain nav | Phase 9 NL3 action list complete locally; clipped sub-polygon carving, path caching, and hierarchical/time-sliced A* remain future refinements |
| PHASE-10 | Animation depth (IK, additive, blend trees, retarget) | done | CO-9 | `test_rt_animcontroller3d` IK two-bone/look-at/FABRIK + **terrain-foot orientation** (`IKSolver3D.SetGroundNormal`, parent-local conversion verified with a rotated parent) + **bone-count LOD** (`SetBoneLOD` freezes distal bones, still following ancestors); `test_rt_skeleton3d` **humanoid role-mapping retarget** (mixamo↔UE bone names) + proportional retarget; true additive, blend trees, anim-rate LOD; `g3d_openworld_slice_probe` skinned play/crossfade + IK | All R-ANIM-001/004 sub-features landed (retarget, bone-count LOD, terrain-foot orientation); deeper humanoid bone coverage is polish |
| PHASE-11 | Asset pipeline depth (compression, KTX2, glTF camera/scene) | partial | PHASE-4 | `test_rt_canvas3d` TextureAsset3D KTX2/material bridge, RGBA8 per-mip fallback switching, material draw-time resolution after residency changes, retained native BC7 mip payloads, native resident mip helper coverage, and mip residency telemetry; `test_vgfx3d_backend_utils` compressed block-row upload budget/pending-byte math; `test_rt_model3d` Model3D scene-indexed API, scene-local glTF camera import, and secondary glTF scene roots; `test_rt_canvas3d` **BC3 (DXT5), BC7 modes 0-7, representative ETC2 RGBA8/EAC, and ASTC LDR void-extent software reference decode** to an RGBA8 Pixels fallback (renders supported compressed textures on non-native backends; unsupported ETC2/ASTC blocks stay native-only, native blocks retained); `g3d_openworld_slice_streaming_hitch_native_compressed_probe` records resident native upload, local Metal ASTC raw-vs-compressed RAM/VRAM reduction, and final-frame tolerance | Basis/Draco/meshopt and broader cross-platform compressed-texture baselines remain |
| PHASE-12 | Open-world vertical slice + tooling + docs + baselines | partial | All API areas usable | `g3d_openworld_slice_probe`; `g3d_openworld_slice_perf_probe`; `g3d_openworld_slice_gpu_smoke`; `g3d_openworld_slice_package_dry_run`; `test_rt_game3d` stream inspection, World3D counter, and navmesh bake hooks | Open-world smoke streams/renders/simulates/replays with visible streamed terrain; stream inspection, streamed-terrain world-scoped navmesh bake, aggregate runtime counters, skinned glTF play/crossfade, LookAt IK binding, terrain-sampled TwoBone foot IK, software visual baseline, capability-gated GPU smoke with degenerate-basis robustness pass, deterministic perf probe, and named local macOS Release software/Metal perf baseline landed; cross-platform perf baselines remain |

## Acceptance criteria

| ID | Criterion | Status | Proof / link | Notes |
|---|---|---|---|---|
| AC-001 | Determinism preserved with job pool on/off, VM/native | todo |  |  |
| AC-002 | Scene 50 km from origin renders/simulates without jitter beyond tolerance | done | `test_rt_scene3d` far-origin AABB/sphere/raycast precision; `test_rt_physics3d` far-origin body integration plus body/contact/query rebase; `test_rt_canvas3d` camera-relative matrix/light/raw-generated upload proof; `test_rt_particles3d_contract` particle rebase; `test_rt_game3d` 50 km camera/body/listener/effect-registry, manual rebase, rendered near/far parity, and flag-off byte-equality proof | Scene/query, physics precision/rebase, matrix-backed plus raw/generated camera-relative upload, standalone particle/sprite hooks, world-owned particle/decal rebase, rendered 50 km parity, and bounded flag-off bytes are covered locally |
| AC-003 | Frame cull scales with visible (not total) node count; recorded speedup | partial | `test_rt_scene3d` 10k generated cull/query fixture | candidate-reduction proof landed; named-hardware timing baseline still todo |
| AC-004 | Streaming stays within bounded per-frame budget; recorded hitch delta | partial | `test_rt_game3d`; `test_rt_gltf`; `test_gif_decode`; `test_rt_scene3d`; `test_rt_canvas3d`; `test_vgfx3d_backend_utils`; `g3d_openworld_slice_streaming_hitch_probe` | Deterministic `WorldStream3D.update(dt)` load budget and `pendingRequestCount` telemetry landed; glTF/GLB root plus external/embedded dependency staging, PNG/BMP/JPEG/GIF image decode to RGBA POD, sliced decoded RGBA-to-`Pixels` commit preparation, static/skinned/morph-target triangle-list/strip/fan/sparse-accessor mesh decode to `Mesh3D`/`MorphTarget3D` POD, required payload preload validation including corrupt texture rejection, texture-aware template cache-budget eviction, `Assets3D.SetResidencyHint` priority/distance cache eviction, mesh/LOD resident-byte controls and stream-cell remeasurement, resident-byte load/clear churn telemetry, and `Canvas3D.TextureUploadBytes`/`TextureUploadPendingBytes` backend upload telemetry plus `SetTextureUploadBudget` controls now happen off or around the commit-side model build; `g3d_openworld_slice_streaming_hitch_probe` records blocking-vs-async timings and proves zero upload budget holds positive-cost async commit work pending until a positive budget restores progress; the named native-compressed hitch rerun is covered locally |
| AC-005 | >4 km² streamed world: bounded memory, no seams | partial | `g3d_openworld_slice_probe`; `g3d_openworld_slice_long_traversal`; `test_rt_game3d`; `test_rt_gltf`; `test_rt_scene3d`; `test_rt_canvas3d` | Open-world probes traverse all four far-apart quadrants repeatedly with one resident cell/tile/collider/nav source and bounded bytes over staged stream ticks; unit tests verify heightfield collider/nav-bake source residency, streamed metadata, stitched adjacent terrain LOD seams with mismatched sidecars, a 9600-unit / >4 km2 multi-tile terrain proof beyond the single-heightmap cap, measured loaded VSCN cell residency for authored base/resident-LOD payloads plus lower bytes after LOD demotion, staged pending loads, streamed terrain rendering through `World3D.drawScene`, and worker-backed async model completion; `long_traversal.zia` now records named software/Metal max visit time, max stream bytes, zero pending requests, replay checksum, and 18,939,904 m2 streamed area; external/embedded glTF dependency staging plus PNG/BMP/JPEG/GIF/static/skinned/morph-target mesh POD decode, decoded texture commit slicing, material/native-compressed texture upload controls, mesh/LOD resident-byte remeasurement, and template-cache resident-byte churn are covered; streamed nav bake/export tooling remains |
| AC-006 | Occlusion + LOD cut draw calls/fill by recorded factor, no missing geo | done | `test_rt_canvas3d`; `test_rt_scene3d`; `g3d_openworld_slice_visibility_dense_probe` | Dense CPU occlusion fixture queues 65 opaque draws, submits one backend draw, and reports 64 skips; indexed Scene3D occlusion fixture narrows 130 drawables to 2 CPU grid candidates and submits one draw; portal/PVS fixture culls an unlinked room and reveals it after an authored portal is added; LOD/impostor correctness is unit-covered; the authored city/forest probe records 169 drawables reduced to 49 submitted draws, 50.407% fill-proxy reduction, and no missing software pixels on the local Release baseline |
| AC-007 | >16 lights render correctly (clustered); CSM stable | done | `test_rt_canvas3d`; `test_rt_canvas3d_gpu_paths`; `g3d_openworld_slice_gpu_smoke` | Software clustered baseline submits >16 active lights behind capability gate; real GPU backends advertise the 64-light clustered/forward+ path; local Metal Release smoke records 24 configured lights against the 16-light fallback and a 3-cascade primary directional CSM pass (`shadow_map=1024`, `draws=4`) |
| AC-008 | Boxes stack/pile rests; body-count target met; hinge/rope/6DOF + true convex | done | A warm-started sequential-impulse solver with sleep islands now lands a stable, deterministic resting box stack (`test_rt_physics3d` `test_box_stack_rests_stably`); also covered: solver-iteration support effect, AABB manifolds, rotated-box clipped OBB manifolds, island-batched contact scheduling and `PHYSICS_ISLAND_BATCH_TARGET` (`bodies=257 piles=32 islands=32`), mesh BVH narrow phase with convex-hull contacts and `PHYSICS_MESH_BVH_TARGET`, GJK/EPA convex-hull + hull-vs-simple contacts with `PHYSICS_CONVEX_GJK_TARGET`, sparse 321-body step stress, hinge/rope/SixDof limits, and SixDof pose-angle stability under spin and linear motor drive | `test_rt_physics3d` 627/627 locally |
| AC-009 | Navmesh auto-bakes + tile-rebuilds; agent-count target paths + avoids | done | `test_rt_navmesh_blend` voxel autogen baker + radius erosion + spatial query-grid parity + retained tile-source rebuild proof + exact footprint carving + traversal metadata; `test_rt_navagent3d` RVO candidate solver, head-on deadlock avoidance, multi-pair crowd cross, O(N) avoidance peer-culling grid-vs-full-scan parity, and `NAVAGENT_CROWD_TARGET: agents=200 frames=180 update_us=564686 min_pair_distance=1.142 crossed=170`; `g3d_openworld_slice_probe` streamed-terrain nav | Voxel autogen, agent-radius erosion, path acceleration (query grid), crowd avoidance, tile-local rebuild with per-tile geometry re-voxelization, exact footprint carving, traversal metadata, and the agent-count baseline landed |
| AC-010 | IK foot-plant + look-at; additive layers, blend trees, retargeting | done | `test_rt_animcontroller3d` IK two-bone/look-at/FABRIK + terrain-foot orientation (`SetGroundNormal`, rotated-parent verified) + bone-count LOD (`SetBoneLOD`); `test_rt_skeleton3d` humanoid role-mapping retarget (mixamo↔UE) + proportional retarget; additive/blend-tree/anim-LOD slices; `g3d_openworld_slice_probe` skinned play/crossfade + IK | Foot-plant, look-at, additive, blend trees, humanoid + proportional retargeting, and bone-count LOD all landed; deeper humanoid bone-role coverage is polish |
| AC-011 | Compressed textures per backend (recorded VRAM); KTX2/precompressed blocks; glTF camera/scene | partial | `test_rt_canvas3d` KTX2 metadata/RGBA8 per-mip material fallback, already-bound material residency switching, retained native BC7 mip payloads, native resident mip helper coverage, plus mip residency byte telemetry; `test_vgfx3d_backend_utils` compressed block-row upload budget/pending-byte math; `test_rt_canvas3d_production` compression capability defaults; `test_rt_model3d` scene-indexed Model3D API, scene-local glTF cameras, and secondary-scene import; `test_rt_canvas3d` BC3, BC7 modes 0-7, representative ETC2 RGBA8/EAC, and ASTC LDR void-extent software decode to RGBA8 fallback; `g3d_openworld_slice_streaming_hitch_native_compressed_probe` records resident native upload, local Metal ASTC raw-vs-compressed RAM/VRAM reduction, and final-frame tolerance | Basis/Draco/meshopt are Phase 11b/stretch; broader cross-platform compressed-texture baselines remain |
| AC-012 | `examples/3d/openworld_slice/` demonstrates the full stack + replay | partial | `g3d_openworld_slice_probe`; `g3d_openworld_slice_perf_probe`; `g3d_openworld_slice_long_traversal`; `g3d_openworld_slice_gpu_smoke` | Software smoke covers all-four-quadrant bounded stream traversal, repeated deterministic residency churn, rendered heightmapped terrain payloads, stream inspection hooks, World3D counter reads, streamed-terrain world-scoped navmesh bake, visible KTX2/BC7 texture asset usage, skinned glTF play/crossfade plus LookAt IK, visible terrain-sampled TwoBone foot IK markers, async asset handle completion, character/physics/nav stepping, final-frame baseline comparison, deterministic replay, software perf metrics, and a named local macOS Release software/Metal perf baseline; GPU smoke is capability-gated and now includes the degenerate-basis robustness pass; cross-platform perf baselines remain |
| AC-013 | Cross-platform + completeness + docs/ctest per API every phase | todo |  |  |
| AC-014 | No regression: bounded-scene game byte-unchanged with flags off | partial | `test_rt_game3d` bounded floating-origin toggle-off byte-equality proof | Broader existing bounded-scene game/sample no-regression remains NL3-032 |

## Current readiness notes

- First implementation slice landed: `World3D.workerCount`,
  `World3D.jobsEnabled`, `World3D.setWorkerCount`, plus
  `test_graphics3d_abi_surface` guarding naming/class-id drift. `World3D` now
  also owns a lazy internal worker pool for animator batches, with
  single-worker vs. multi-worker parity covered in `test_rt_game3d`.
  `g3d_3dnext2_surface_probe` now covers the Phase-0 ordered parallel-map
  surface and a two-run `World3D.runFramesOnly` worker-count replay. Phase C
  still must close CO-1 (cross-platform) and the Windows/Linux side of CO-11
  before the plan can record full cross-platform baselines.
- Floating-origin public controls also landed (`floatingOrigin`, `worldOrigin`,
  `setOriginRebaseThreshold`, and `rebaseOrigin`) with flag-off,
  threshold-rebase, manual-rebase, and active-frame rejection coverage.
  Scene3D indexed spatial/query bounds and Physics3D body integration now have
  far-origin precision regressions. Game3D floating-origin frames enable a
  Canvas3D camera-relative upload path for camera payloads, double `DrawMesh`
  model matrices, identity raw meshes, generated particle/sprite/decal vertices,
  and point/spot lights; registered particles/decals and standalone
  particle/sprite/physics query-contact state now shift with a rebase. The
  50 km rendered near/far parity and bounded flag-off byte equality proofs now
  land in `test_rt_game3d`; the broader existing bounded-scene game/sample
  no-regression proof remains NL3-032.
- `Scene3D.RebaseOrigin` now provides the raw scene-subtree rebase primitive,
  and Game3D floating origin calls through it while routing physics through
  `Physics3DWorld.RebaseOrigin` before shifting camera, listener state, and
  world-owned effects.
- Game3D collision wrappers now expose `contactCount`, `contactPoint(i)`,
  `contactNormal(i)`, and `contactSeparation(i)` with ctest + ABI-name
  coverage. `Physics3DWorld.SolverIterations` / `SetSolverIterations` also
  landed with a spring-solver effect test. Raw multi-point manifolds and the
  warm-started contact solver remain Phase 8.
- Mesh-vs-sphere/capsule/box/convex-hull narrow phase now traverses the
  per-mesh physics BVH after the body-centric broadphase selects body
  candidates, keeping the full triangle scan as a fallback; `test_rt_physics3d`
  asserts the BVH is built by sphere/box mesh contacts and records
  `PHYSICS_MESH_BVH_TARGET` with 1 built mesh BVH across 16 static tiles.
- Sequencing override (pull Phase 8/9 earlier) is decision SEQ in
  `02-decisions.md`.
- A 2026-05-29 source-comparison pass re-verified every `file:line` claim and
  corrected the plan to the current subsystem layout (`scene/`, `physics/`, …);
  see `../review.md` §"Source-comparison pass". Per-backend checklists now cover
  all three GPU paths: `../metal.md` (macOS), `../directx.md` (Windows),
  `../opengl.md` (Linux). GATE-003's mac/win/linux lane maps to these three.
