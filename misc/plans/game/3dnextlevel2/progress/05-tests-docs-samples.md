# Tests, docs, samples tracker

Required ctests, docs pages, fixtures, and the vertical slice from
`../roadmap.md`. All rows start `todo`.

## Required ctests (per phase)

| ID | Area | Coverage | Source | Status | Proof / link | Notes |
|---|---|---|---|---|---|---|
| TEST-C1 | Carryover | Cross-platform `-L graphics3d` green mac/win/linux | CO-1 | partial | Local macOS `VIPER_SKIP_INSTALL=1 ./scripts/build_viper_mac.sh` passes, including `graphics3d` 75/75 | Windows/Linux host runs remain prerequisite |
| TEST-C2 | Carryover | Perf lane (software FPS + GPU smoke) | CO-11 | partial | Release macOS Apple M4 Max lane passes `g3d_openworld_slice_perf_probe` and `g3d_openworld_slice_gpu_smoke`; `g3d_openworld_slice_perf_harness` validates required perf counters; direct Release `perf_probe.zia` records software and Metal measurements in `baselines/perf_macos_apple_m4_max.md` | Windows/Linux named-hardware FPS/perf lanes remain prerequisite |
| TEST-C3 | Carryover | Render-target finalization + committed fixture sample | CO-4/CO-9 | done | `g3d_openworld_slice_probe` covers committed skinned glTF play/crossfade plus LookAt IK binding, committed GLB loading through `Assets3D.LoadModelAsset`, and committed WAV loading through `Sound3D.loadAsset`; finalization remains covered by Canvas3D/Game3D probes |  |
| TEST-C4 | Carryover | Lifetime/diagnostics + leak probes | CO-3 | done | `test_rt_game3d`; `g3d_test_game3d_destroyed_handle_reject`; `g3d_test_game3d_destroyed_entity_reject`; `g3d_test_game3d_double_despawn_reject`; `g3d_test_game3d_assets_probe`; `g3d_test_canvas3d_quality_profiles` cover method-qualified destroyed-handle diagnostics, double-despawn, missing package-asset terminal errors, imported-group spawn/despawn churn, cache clear/reload, and quality fallback reasons | General ASAN/Valgrind lanes remain optional validation |
| TEST-C5 | Carryover | CO-12 Phase-1 exit partials | CO-12 | done | `test_rt_game3d` fixed-loop accumulator/spiral guard and packaged glTF hierarchy; `g3d_test_game3d_world_probe` direct `Entity3D.FromNode`, synthetic `tick`, clamped `stepSimulation`, final capture, teardown; `g3d_test_game3d_destroyed_handle_reject`; `g3d_game3d_hello`; `g3d_game3d_common_no_mat4`; `g3d_game3d_starter_probe`; `g3d_test_game3d_docs_snippets` | Broader worker/VM determinism remains Phase 1 |
| TEST-0 | Foundations | Worker-job + deterministic two-run replay; perf fixtures | Phase 0 | partial | `g3d_3dnext2_surface_probe` covers worker-backed ordered map replay and `World3D.runFramesOnly` worker-count parity; `g3d_openworld_slice_perf_harness` validates perf-probe counters and emits a stable `HARNESS:` summary; `test_graphics3d_abi_surface`; `test_rt_game3d` worker-control and animator-batch parity slices; open-world Release macOS perf baseline recorded | Windows/Linux perf fixtures remain |
| TEST-1 | Concurrency | Parallel-map determinism; race/stress; pool on/off parity | Phase 1 | partial | `g3d_3dnext2_surface_probe` covers ordered `Parallel.MapPool` replay and `World3D` worker-count replay parity; `test_rt_g3d_commit_queue` covers FIFO drains, item/cost-budgeted drains, zero-cost work under zero budget, oversized-item liveness, worker-enqueue/main-thread-commit behavior, and worker-drain rejection; `test_rt_game3d` covers public worker controls and world-pool animator batch parity; `test_rt_parallel` / `test_rt_parallel_reduce` cover the general parallel runtime | Race-stress/TSan and native-compressed backend upload proof remain |
| TEST-2 | Floating origin | Far/near tolerance; rebase determinism; flag-off equality | Phase 2 | partial | `test_rt_scene3d` covers raw scene subtree rebase plus far-origin AABB/sphere/raycast precision at `1e9`; `test_rt_game3d` covers flag-off unchanged, camera/scene/body/listener threshold rebase, registered particle/decal draw centers before and after 50 km rebasing, and `World3D.floatingOrigin` enabling camera-relative upload for frames; `test_rt_physics3d` covers far-origin body integration preserving a sub-float `0.125` step delta; `test_rt_canvas3d` covers camera-relative frame payloads, double `DrawMesh` model translation, and point-light rebasing | 50 km rendered parity, standalone raw-particle/in-flight-query atomic hooks, and byte-equality proof still todo |
| TEST-3 | Spatial index | Cull/query equality vs flat walk; scaling benchmark | Phase 3 | partial | `test_rt_scene3d` covers indexed query rebuild/reuse, dirty-node refit, draw parity vs flat traversal, candidate prefilter telemetry, and a generated 10k cull/query scaling fixture | named-hardware timing benchmark still todo |
| TEST-4 | Streaming I/O | Hitch budget; async==blocking equality; no-leak residency | Phase 4 | partial | `test_rt_game3d` covers `AssetHandle3D` worker-scheduled entity/template parity, pending progress, pre-observation/worker-pending cancel terminal state, completed-handle cancel stability, missing filesystem/asset load-error handles, unsupported-extension terminal errors, malformed glTF JSON terminal errors, corrupt required texture-image terminal errors, zero upload budget keeping a positive-cost textured commit pending until a positive budget is restored, large decoded texture commits exposing partial upload progress before final publish, background `Assets3D.Preload` and `Assets3D.PreloadAsset` cache warming through world-drained commits, corrupt preload no-trap behavior, explicit template eviction, zero-budget cache eviction, texture-backed template budget eviction when decoded material pixels exceed the budget, `Assets3D.GetResidentBytes` resident-byte telemetry returning to zero after blocking and async load/clear churn, and stale-preload `ClearCache` generation guarding; `test_rt_gltf` proves a preloaded glTF bundle can supply an external `.bin` buffer after the source file is removed, stage data URI buffer/image payloads, worker-decode PNG, BMP, JPEG, and GIF image payloads into raw RGBA POD, prepare decoded RGBA POD into `Pixels` across bounded commit slices, and worker-decode static/skinned/morph-target glTF triangle-list/strip/fan mesh primitives with positions, optional normals, sparse accessor overrides, JOINTS_1 top-four reduction, skin remapping, morph deltas, and node morph weights into raw `Mesh3D`/`MorphTarget3D` POD, regenerate missing normals during commit, stage bufferView image payloads, reject missing required buffers, reject accessor byte ranges that overrun bufferViews during preload, and reject corrupt required data-URI texture images in both preload and blocking load; `test_rt_g3d_commit_queue` covers cost-budgeted FIFO drains; `test_rt_canvas3d` covers `Canvas3D.SetTextureUploadBudget`, `TextureUploadPendingBytes`, and `TextureUploadBytes`; `test_vgfx3d_backend_utils` covers shared RGBA/cubemap upload byte accounting, pending-byte return-to-baseline proof, cubemap row-slice unpacking, and row-budget math; `test_gif_decode` covers raw GIF first-frame decode; `g3d_openworld_slice_streaming_hitch_probe` records blocking-vs-async template timing, proves zero-upload-budget pending/release behavior, and verifies blocking/async resident-byte churn; `g3d_openworld_slice_probe` waits for true async completion instead of relying on synchronous first observation; ABI/Model3D/link guards cover names, glTF parser recovery, and internal preloaded-loader surface classification | native-compressed backend upload slicing still todo |
| TEST-5 | World partition | Tile/cell stream in/out; seam; bounded-memory; determinism | Phase 5 | partial | `test_rt_game3d` covers `World3D.stream` ownership, `WorldStream3D` mounted manifest telemetry, manifest-driven `.vscn` scene-cell load/unload across boundaries, measured loaded-cell mesh/LOD/impostor/material texture residency with budget eviction over manifest estimates, manifest-driven rendered heightmapped `Terrain3D` tile payload load/unload, per-tile heightfield collider body residency, hidden stream-owned nav-bake source residency, deterministic per-update load budgeting with `pendingRequestCount`, shared-edge seam sampling, resident byte accounting, world nav bake inclusion, and zero/unlimited budget behavior; `g3d_openworld_slice_probe` covers all-four-quadrant bounded tile/cell/collider/nav-source swaps and the software baseline includes visible streamed terrain; `g3d_openworld_slice_long_traversal` repeats all-quadrant stream churn with deterministic replay; ABI/link guards cover names/class id | native-compressed backend upload proof and named hitch proof remain |
| TEST-6 | Visibility | Occlusion skip counts; LOD/impostor stability | Phase 6 | partial | `test_rt_canvas3d` covers dense covered-opaque occlusion reduction by queuing 65 opaque draws, submitting one backend draw, and reporting 64 skips; `test_rt_canvas3d_gpu_paths` covers off-frustum rejection; `test_rt_scene3d` covers visibility telemetry, auto-LOD, impostor proxy draw; ABI guard covers names | named-hardware timing/fill benchmark and portal/PVS remain |
| TEST-7 | Lighting | >16-light correctness; CSM stability; fallback | Phase 7 | partial | `test_rt_canvas3d` covers software clustered capability, >16-light submission, and unsupported backend traps; `test_rt_canvas3d_gpu_paths` covers 16-light forward payload fallback and `CastsShadows=false` shadow-selection exclusion; `test_graphics3d_abi_surface` guards API names | GPU clustered correctness and CSM stability still todo |
| TEST-8 | Physics | Stacking/rest; manifold; GJK; hinge/rope/6DOF; body-count perf | Phase 8 | partial | `test_rt_game3d` covers Game3D contact accessors/fallbacks; `test_rt_physics3d` covers solver-iteration clamps/effect, AABB manifold count >1, convex-hull GJK/EPA overlap, AABB false-positive rejection, hull-vs-simple contained sphere detection, sparse 321-body step stress, mesh narrow-phase BVH build/use for sphere and box contacts, and hinge/rope/SixDof limits; `test_graphics3d_abi_surface` guards names | non-AABB manifolds, solver stability, richer 6DOF pose-angle coverage, resting-pile target, and named perf still todo |
| TEST-9 | Navigation | Autogen equivalence; tile rebuild; avoidance bounds; agent perf | Phase 9 | partial | `test_rt_navagent3d` covers avoidance property clamps and head-on desired-velocity reduction; `test_rt_navmesh_blend` covers scene `Bake`, `BakeTiled`, whole-mesh `RebuildTile`, off-mesh link bridging, directed traversal, endpoint validation, count telemetry, agent-radius portal gating, and `AddObstacle`/`UpdateObstacle`/`RemoveObstacle` coarse AABB carving edits; `test_rt_game3d` and `g3d_openworld_slice_probe` verify world-scoped nav bake sees resident streamed-terrain source nodes; ABI/link guards cover public surface | voxel/region autogen equivalence, tile-local rebuild, full avoidance bounds, full corridor erosion, and agent-count perf remain |
| TEST-10 | Animation | IK error bounds; additive; retarget; anim-LOD | Phase 10 | partial | `test_rt_animcontroller3d` covers `IKSolver3D` two-bone target error/weight blending/clearing, look-at local +Z aiming, FABRIK chain bounds, true additive bind-pose delta composition versus replace overlay, `CrossfadeLayerAdditive` blending, deterministic `SetAnimationLOD` throttling, and `SetBlendTree` controller base-pose driving; `test_rt_game3d` covers `Animator3D.playLayerAdditive`, `crossfadeLayerAdditive`, event capture, `Animator3D.setBlendTree`, and `Animator3D.setIKSolver`; `test_rt_navmesh_blend` covers `BlendTree3D` 1D/2D weights and bad-handle rejection; `test_rt_skeleton3d` covers `Animation3D.Retarget` name-based mapping, metadata preservation, and bad handles; `g3d_openworld_slice_probe` covers committed imported skinned play/crossfade plus LookAt IK binding and terrain-sampled TwoBone foot IK; ABI/link guards cover public methods | humanoid/proportional retarget mapping, bone-count LOD, and terrain-foot orientation/visible-character polish remain |
| TEST-11 | Asset pipeline | Compressed tolerance per backend; KTX2/precompressed blocks; glTF camera/scene | Phase 11 | partial | `test_rt_canvas3d` covers `TextureAsset3D.LoadKTX2` RGBA8 metadata/per-mip fallback switching, material binding, already-bound material resolution after residency changes, compressed-only rejection, retained native BC7 mip payloads, and mip residency byte telemetry; `test_vgfx3d_backend_utils` covers compressed block-row upload budgeting/pending-byte math; `test_rt_canvas3d_production` covers `bc7`/`astc`/`etc2` capability defaults; `test_rt_model3d` covers the `Model3D` scene-indexed API contract, scene-local glTF perspective/orthographic camera import, and secondary glTF scene instantiation; ABI/link guards cover public names | Basis/Draco/meshopt covered by optional Phase 11b tests; native compressed upload/backend mip residency remain |
| TEST-12 | Vertical slice | Smoke; deterministic replay; software baseline; perf baselines | Phase 12 | partial | `g3d_openworld_slice_probe` covers all-four-quadrant bounded stream traversal, stream inspection hooks, World3D entity/body/draw/visibility/stream counters, world-scoped navmesh bake including streamed terrain, rendered heightmapped terrain payload access and collider residency, KTX2 texture asset residency, skinned glTF play/crossfade plus LookAt IK binding, committed GLB/WAV fixture loads, terrain-sampled TwoBone foot IK, async model handle completion, character/physics/nav stepping, committed software final-frame baseline comparison, and deterministic replay; `g3d_openworld_slice_long_traversal` repeats all-quadrant residency churn and replay checks; `g3d_openworld_slice_perf_probe` records deterministic software perf metrics; `g3d_openworld_slice_perf_harness` validates/normalizes perf metrics for CI; `g3d_openworld_slice_gpu_smoke` covers capability-gated GPU backend execution plus a degenerate-basis normal-map robustness pass; Release macOS software/Metal perf baselines are recorded; package dry-run covers sample asset layout | native compressed upload proof and Windows/Linux perf remain |

## Fixtures

| ID | Fixture | Source | Status | Proof / link | Notes |
|---|---|---|---|---|---|
| FIX-001 | 10k-node scene (cull scaling) | Phase 0 | done | `test_rt_scene3d` generated 100x100 drawable-node grid | covers Scene3D cull/query candidate scaling; Release open-world perf lane now records named local wall-clock timing |
| FIX-002 | >4 km² terrain/world stand-in (streaming) | Phase 0/5 | partial | `examples/3d/openworld_slice/assets/world/terrain.vscn` declares four far-apart 65x65 tiles at 34m scale with committed height sidecars and CTest stream-center swaps; resident tiles render as terrain meshes, spawn matching heightfield collider entities, contribute hidden nav-bake source nodes, and repeat all-quadrant churn in `long_traversal.zia` over staged stream ticks | native-compressed backend upload proof and hitch instrumentation remain |
| FIX-003 | Dense city/forest (occlusion/LOD) | Phase 6 | partial | `test_rt_canvas3d` generated dense covered-draw fixture | authored city/forest scene and named-hardware reduction baseline remain |
| FIX-004 | Many-light scene (clustered/CSM) | Phase 7 | partial | `test_rt_canvas3d` generated 24-light clustered/software fixture | authored many-light scene, GPU clustered baseline, and CSM fixture remain |
| FIX-005 | Body-count stress (physics) | Phase 8 | partial | `test_rt_physics3d` sparse 321-body step stress plus >128 contact/event storage growth | named perf target and resting pile stress remain |
| FIX-006 | Agent-count nav scene | Phase 9 | todo |  |  |
| FIX-007 | Committed skinned character + GLB/glTF-deps + audio | CO-9 | done | `examples/3d/openworld_slice/assets/models/skinned_agent.gltf` plus `skinned_agent.bin`, `assets/models/triangle.glb`, and `assets/audio/jump.wav`; `g3d_openworld_slice_probe`; `g3d_openworld_slice_package_dry_run` | Fixtures are synthetic/redistributable or copied from existing repository assets. |
| FIX-008 | Compressed-texture / KTX2/precompressed assets | Phase 11 | partial | `test_rt_canvas3d` writes minimal KTX2 RGBA8 and BC7 fixtures at test time; `examples/3d/openworld_slice/assets/textures/` commits tiny RGBA8 and BC7 KTX2 fixtures for sample/package coverage | representative texture corpus, native compressed upload fixtures, and Phase 11b fixtures remain |

## Docs

| ID | Page | Source | Status | Proof / link | Notes |
|---|---|---|---|---|---|
| DOC-001 | Concurrency + determinism model | Phase 1 | done | `docs/viperlib/graphics/game3d.md` worker-control section; `docs/graphics3d-architecture.md` threading model documents copied-data worker jobs, deterministic ordered merge, main-thread-only Graphics3D/Game3D handle mutation, and the commit-queue boundary | Future asset decode/upload docs reopen under DOC-010 / Phase 4 |
| DOC-002 | Large worlds: floating origin + streaming + partition | Phase 2/5 | partial | `docs/viperlib/graphics/game3d.md` covers floating-origin API, `World3D.stream`, `World3D` aggregate telemetry counters, `WorldStream3D` cell and terrain manifest schemas including height sidecars, staged `update(dt)` load budgeting and `pendingRequestCount`, rendered terrain payload/collider residency telemetry, and the `openworld_slice` CTest smoke | native-compressed backend upload proof and broader perf guidance still todo |
| DOC-003 | Visibility + lighting scaling | Phase 6/7 | partial | `docs/viperlib/graphics/rendering3d.md` covers Scene3D indexed visibility/query behavior, occlusion controls, LOD/impostors, clustered-light gates, the software many-light baseline, and shadow gates | architecture page and deeper backend notes still todo |
| DOC-004 | Physics depth (joints, solver, convex) | Phase 8 | partial | `docs/viperlib/graphics/physics3d.md` covers solver iterations, BVH-backed triangle-mesh candidate pruning, convex-hull GJK/EPA hull/simple behavior, sleeping controls, sparse body-count stress, and hinge/rope/SixDof limits | richer 6DOF pose-angle docs and warm-start/island docs remain |
| DOC-010 | Async assets | Phase 4 | partial | `docs/viperlib/graphics/game3d.md` covers `Assets3D.LoadModel*Async`, `AssetHandle3D` worker-scheduled/cancel/error behavior including missing-path, unsupported-extension, malformed glTF JSON failures, corrupt required texture payload failures, glTF/GLB root plus external/embedded buffer/image staging, worker-side PNG/BMP/JPEG/GIF image decode to raw RGBA POD plus bounded commit-side `Pixels` preparation, worker-side static/skinned/morph-target glTF triangle-list/strip/fan mesh decode to raw `Mesh3D`/`MorphTarget3D` POD with commit-side normal regeneration, skin remapping, and morph attachment, required-buffer/accessor-range/corrupt-texture preload validation, background filesystem/package-aware `Preload` / `PreloadAsset` through world-drained commits, `SetUploadBudget` decoded-image commit-slice gating, `SetResidencyBudget` texture-pixel accounting, `GetResidentBytes` resident-byte cache telemetry, `ClearCache` generation guarding, and `Evict`; `examples/3d/openworld_slice/README.md` documents the streaming hitch probe; `docs/graphics3d-guide.md` and `docs/viperlib/graphics/rendering3d.md` document `Canvas3D.SetTextureUploadBudget`, `TextureUploadBytes`, and `TextureUploadPendingBytes`; `runtime-changes.md` calls out the remaining native-compressed backend upload slicing work and local resident-byte/backend pending-byte proof | native-compressed backend upload docs remain |
| DOC-005 | Navigation + AI depth | Phase 9 | partial | `docs/graphics3d-guide.md` and `docs/viperlib/graphics/rendering3d.md` document `NavMesh3D.Bake`, `BakeTiled`, `RebuildTile`, `AddOffMeshLink`/`OffMeshLinkCount`, `AddObstacle`/`UpdateObstacle`/`RemoveObstacle`/`ObstacleCount`, the `agentRadius` portal gate, and `NavAgent3D` avoidance controls; `docs/viperlib/graphics/game3d.md` documents `World3D.bakeNavMesh` / `bakeTiledNavMesh` and resident streamed-terrain bake source participation | voxel/region autogen, tile-local rebuild, full corridor erosion, full crowd solver, and perf guidance remain |
| DOC-006 | Animation depth (IK, additive, blend trees, retarget) | Phase 10 | partial | `docs/graphics3d-guide.md` and `docs/viperlib/graphics/rendering3d.md` document `IKSolver3D`, `AnimController3D.SetIKSolver`, `AnimController3D.PlayLayerAdditive`, `CrossfadeLayerAdditive`, `SetAnimationLOD`, `SetBlendTree`, `BlendTree3D`, and `Animation3D.Retarget`; `docs/viperlib/graphics/game3d.md` documents `Animator3D.playLayerAdditive`, `crossfadeLayerAdditive`, `setBlendTree`, `setIKSolver`, and the open-world terrain-foot IK probe | humanoid/proportional retarget mapping, terrain-foot orientation guidance, and deeper animation LOD docs remain |
| DOC-007 | Asset pipeline (compression, KTX2, glTF camera/scene) | Phase 11 | partial | `docs/graphics3d-guide.md` documents `TextureAsset3D`, mip residency telemetry, active RGBA8 mip fallback binding with material draw-time resolution, retained native compressed mip payloads, backend compression gates, `Model3D` scene-indexed APIs, scene-local glTF cameras, and secondary glTF scenes; `docs/viperlib/graphics/rendering3d.md` covers material/capability behavior, texture residency telemetry, and the Model3D camera/scene contract | native compressed upload/backend residency remain |
| DOC-008 | Update `docs/graphics3d-architecture.md` + `docs/viperlib/graphics/` pages | Phase 12 | todo |  |  |
| DOC-009 | Every page has a ctest-compiled snippet | All | todo |  |  |

## Sample

| ID | Item | Source | Status | Proof / link | Notes |
|---|---|---|---|---|---|
| SLICE-001 | `examples/3d/openworld_slice/` full-stack sample | Phase 12 | partial | `g3d_openworld_slice_probe`; `g3d_openworld_slice_perf_probe`; `g3d_openworld_slice_perf_harness`; `g3d_openworld_slice_long_traversal`; `g3d_openworld_slice_gpu_smoke`; `g3d_openworld_slice_package_dry_run` | streams/renders/simulates/replays in software across all four stream quadrants with bounded cell/tile/collider/nav-source residency, repeated deterministic residency churn, visible heightmapped terrain payloads, streamed-terrain world nav bake inclusion, KTX2 `TextureAsset3D` material/residency fixtures, imported skinned play/crossfade plus LookAt IK binding, committed GLB/WAV fixture load coverage, terrain-sampled TwoBone foot IK, a committed software visual baseline, deterministic perf probe with reusable harness parsing, named local macOS Release software/Metal perf baseline, and capability-gated GPU smoke with a degenerate-basis normal-map pass; native compressed upload proof remains |
| SLICE-002 | Editor runtime hooks consumed by a probe | Phase 12 | partial | `test_rt_game3d`; `test_rt_canvas3d`; `g3d_openworld_slice_probe`; `g3d_openworld_slice_perf_probe` | `WorldStream3D` cell/tile entry inspection, including terrain heightmap sidecar paths, `World3D` aggregate perf/editor counters, and world-scoped navmesh bake trigger with streamed terrain landed; cross-platform perf capture remains; editor UI stays in ViperIDE roadmap |
| SLICE-003 | Deterministic replay + software visual baseline | Phase 12 | done | `g3d_openworld_slice_probe`; `examples/3d/openworld_slice/assets/baselines/openworld_slice_software.png` | named-hardware perf stays under `P12-004` |
