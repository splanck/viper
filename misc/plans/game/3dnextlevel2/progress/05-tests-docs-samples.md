# Tests, docs, samples tracker

Required ctests, docs pages, fixtures, and the vertical slice from
`../roadmap.md`. All rows start `todo`.

## Required ctests (per phase)

| ID | Area | Coverage | Source | Status | Proof / link | Notes |
|---|---|---|---|---|---|---|
| TEST-C1 | Carryover | Cross-platform `-L graphics3d` green mac/win/linux | CO-1 | partial | Local macOS `VIPER_SKIP_INSTALL=1 ./scripts/build_viper_mac.sh` passes, including `graphics3d` 75/75 | Windows/Linux host runs remain prerequisite |
| TEST-C2 | Carryover | Perf lane (software FPS + GPU smoke) | CO-11 | partial | Release macOS Apple M4 Max lane passes `g3d_openworld_slice_perf_probe` and `g3d_openworld_slice_gpu_smoke`; `g3d_openworld_slice_perf_harness` validates required perf counters; direct Release `perf_probe.zia` records software and Metal measurements in `baselines/perf_macos_apple_m4_max.md` | Windows/Linux named-hardware FPS/perf lanes remain prerequisite |
| TEST-C3 | Carryover | Render-target finalization + committed fixture sample | CO-4/CO-9 | done | `g3d_openworld_slice_probe` covers committed skinned glTF play/crossfade plus LookAt IK binding, committed GLB loading through `Assets3D.LoadModelAsset`, and committed WAV loading through `Sound3D.loadAsset`; finalization remains covered by Canvas3D/Game3D probes |  |
| TEST-C4 | Carryover | Lifetime/diagnostics + leak probes | CO-3 | done | `test_rt_game3d`; `g3d_test_game3d_destroyed_handle_reject`; `g3d_test_game3d_destroyed_entity_reject`; `g3d_test_game3d_double_despawn_reject`; `g3d_test_game3d_assets_probe`; `g3d_test_canvas3d_quality_profiles` cover method-qualified destroyed-handle diagnostics, double-despawn, missing package-asset terminal errors, imported-group spawn/despawn churn, cache clear/reload, and quality fallback reasons | General ASAN/Valgrind lanes remain optional validation |
| TEST-C5 | Carryover | CO-12 Phase-1 exit partials | CO-12 | done | `test_rt_game3d` fixed-loop accumulator/spiral guard and packaged glTF hierarchy; `g3d_test_game3d_world_probe` direct `Entity3D.FromNode`, synthetic `tick`, clamped `stepSimulation`, final capture, teardown; `g3d_test_game3d_destroyed_handle_reject`; `g3d_game3d_hello`; `g3d_game3d_common_no_mat4`; `g3d_game3d_starter_probe`; `g3d_test_game3d_docs_snippets` | Broader worker/VM determinism remains Phase 1 |
| TEST-0 | Foundations | Worker-job + deterministic two-run replay; perf fixtures | Phase 0 | partial | `g3d_3dnext2_surface_probe` covers worker-backed ordered map replay and `World3D.runFramesOnly` worker-count parity; `g3d_openworld_slice_perf_harness` validates perf-probe counters and emits a stable `HARNESS:` summary; `test_graphics3d_abi_surface`; `test_rt_game3d` worker-control and animator-batch parity slices; open-world Release macOS perf baseline recorded | Windows/Linux perf fixtures remain |
| TEST-1 | Concurrency | Parallel-map determinism; race/stress; pool on/off parity | Phase 1 | done | `g3d_3dnext2_surface_probe` covers ordered `Parallel.MapPool` replay and `World3D` worker-count replay parity; `test_rt_g3d_commit_queue` covers FIFO drains, item/cost-budgeted drains, zero-cost work under zero budget, oversized-item liveness, worker-enqueue/main-thread-commit behavior, and worker-drain rejection; `test_rt_game3d` covers public worker controls and world-pool animator batch parity; `test_rt_parallel` / `test_rt_parallel_reduce` cover the general parallel runtime; `scripts/g3d_tsan_concurrency_lane.sh --no-configure` passed 9/9 under ThreadSanitizer on Apple M4 Max covering worker pool, ordered merge, asset workers, commit queue, and open-world streaming hitch probe | Phase-1 race/stress lane closed locally; formal VM/native determinism acceptance remains NL3-031 |
| TEST-2 | Floating origin | Far/near tolerance; rebase determinism; flag-off equality | Phase 2 | partial | `test_rt_scene3d` covers raw scene subtree rebase plus far-origin AABB/sphere/raycast precision at `1e9`; `test_rt_game3d` covers flag-off unchanged, camera/scene/body/listener threshold and manual rebase, registered particle/decal draw centers before and after 50 km rebasing, active-frame rebase rejection, `World3D.floatingOrigin` enabling camera-relative upload for frames, rendered near-origin vs. 50 km final-frame parity with frustum culling on/off, and bounded flag-off byte equality; `test_rt_physics3d` covers far-origin body integration preserving a sub-float `0.125` step delta plus body/contact/query rebase invalidation; `test_rt_particles3d_contract` covers standalone emitter/live-particle rebase; `test_rt_canvas3d` covers camera-relative frame payloads, double `DrawMesh` model translation, raw identity-mesh vertex rebasing, standalone `Particles3D` / `Sprite3D` generated vertex rebasing, explicit `Sprite3D.RebaseOrigin`, and point-light rebasing | broader existing bounded-scene sample no-regression remains NL3-032 |
| TEST-3 | Spatial index | Cull/query equality vs flat walk; scaling benchmark | Phase 3 | partial | `test_rt_scene3d` covers indexed query rebuild/reuse, dirty-node refit, draw parity vs flat traversal, candidate prefilter telemetry, and a generated 10k cull/query scaling fixture | named-hardware timing benchmark still todo |
| TEST-4 | Streaming I/O | Hitch budget; async==blocking equality; no-leak residency | Phase 4 | partial | `test_rt_game3d` covers `AssetHandle3D` worker-scheduled entity/template parity, pending progress, pre-observation/worker-pending cancel terminal state, completed-handle cancel stability, missing filesystem/asset load-error handles, unsupported-extension terminal errors, malformed glTF JSON terminal errors, corrupt required texture-image terminal errors, zero upload budget keeping a positive-cost textured commit pending until a positive budget is restored, large decoded texture commits exposing partial progress before final publish, background `Assets3D.Preload` and `Assets3D.PreloadAsset` cache warming through world-drained commits, corrupt preload no-trap behavior, explicit template eviction, zero-budget cache eviction, texture-backed template budget eviction when decoded material pixels exceed the budget, `Assets3D.SetResidencyHint` priority/distance template eviction, mesh/LOD residency remeasurement for loaded stream cells, `Assets3D.GetResidentBytes` resident-byte telemetry returning to zero after blocking and async load/clear churn, and stale-preload `ClearCache` generation guarding; `test_rt_scene3d` covers nonresident LOD fallback and `Mesh3D.ResidentBytes`; `test_rt_gltf` proves a preloaded glTF bundle can supply an external `.bin` buffer after the source file is removed, stage data URI buffer/image payloads, worker-decode PNG, BMP, JPEG, and GIF image payloads into raw RGBA POD, prepare decoded RGBA POD into `Pixels` across bounded commit slices, and worker-decode static/skinned/morph-target glTF triangle-list/strip/fan mesh primitives with positions, optional normals, sparse accessor overrides, JOINTS_1 top-four reduction, skin remapping, morph deltas, and node morph weights into raw `Mesh3D`/`MorphTarget3D` POD, regenerate missing normals during commit, stage bufferView image payloads, reject missing required buffers, reject accessor byte ranges that overrun bufferViews during preload, and reject corrupt required data-URI texture images in both preload and blocking load; `test_rt_g3d_commit_queue` covers cost-budgeted FIFO drains; `test_rt_canvas3d` covers `Canvas3D.SetTextureUploadBudget`, `TextureUploadPendingBytes`, and `TextureUploadBytes`; `test_vgfx3d_backend_utils` covers shared RGBA/cubemap/native-compressed upload byte accounting, pending-byte return-to-baseline proof, cubemap row-slice unpacking, and row-budget math; `test_gif_decode` covers raw GIF first-frame decode; `g3d_openworld_slice_streaming_hitch_probe` records blocking-vs-async template timing, proves zero-upload-budget pending/release behavior, and verifies blocking/async resident-byte churn; `g3d_openworld_slice_probe` waits for true async completion instead of relying on synchronous first observation; ABI/Model3D/link guards cover names, glTF parser recovery, and internal preloaded-loader surface classification | native-compressed hitch proof is covered locally |
| TEST-5 | World partition | Tile/cell stream in/out; seam; bounded-memory; determinism | Phase 5 | partial | `test_rt_game3d` covers `World3D.stream` ownership, `WorldStream3D` mounted manifest telemetry, manifest-driven `.vscn` scene-cell load/unload across boundaries, measured loaded-cell base/resident-LOD/impostor/material texture residency with budget eviction over manifest estimates, lower measured bytes after LOD demotion, stream material/collision/nav/traversal/sidecar metadata inspection, manifest-driven rendered heightmapped `Terrain3D` tile payload load/unload, per-tile heightfield collider body residency, hidden stream-owned nav-bake source residency, deterministic per-update load budgeting with `pendingRequestCount`, stitched adjacent LOD-seam repair from mismatched sidecars, a 9600-unit / >4 km2 terrain proof beyond one heightmap cap, resident byte accounting, world nav bake inclusion, and zero/unlimited budget behavior; `g3d_openworld_slice_probe` covers all-four-quadrant bounded tile/cell/collider/nav-source swaps and the software baseline includes visible streamed terrain; `g3d_openworld_slice_long_traversal` repeats all-quadrant stream churn with deterministic replay and emits named hitch/memory telemetry; ABI/link guards cover names/class id | streamed nav bake/export tooling remains |
| TEST-6 | Visibility | Occlusion skip counts; LOD/impostor stability | Phase 6 | done | `test_rt_canvas3d` covers dense covered-opaque occlusion reduction by queuing 65 opaque draws, submitting one backend draw, reporting 64 skips, and exposing `OcclusionCandidateCount`; `test_rt_scene3d` covers indexed CPU occlusion candidate selection by narrowing 130 drawables to 2 grid candidates before Canvas3D sorting, authored portal/PVS by culling an unlinked room and revealing it through a new portal, plus visibility telemetry, auto-LOD, and impostor proxy draw; `test_rt_canvas3d_gpu_paths` covers off-frustum rejection; `g3d_openworld_slice_visibility_dense_probe` records the authored city/forest draw/fill-proxy reduction and final-frame no-missing-geometry proof; ABI guard covers names |  |
| TEST-7 | Lighting | >16-light correctness; CSM stability; fallback | Phase 7 | done | `test_rt_canvas3d` covers software clustered/CSM capability, real platform GPU capability, >16-light submission, and unsupported backend traps; `test_rt_canvas3d_gpu_paths` covers 16-light forward payload fallback, `CastsShadows=false` shadow-selection exclusion, and three contiguous CSM cascade slots with monotonic split payloads; `g3d_openworld_slice_gpu_smoke` records a 24-light GPU clustered draw against the 16-light fallback and a 3-cascade Metal CSM fixture; `test_graphics3d_abi_surface` guards API names |  |
| TEST-8 | Physics | Stacking/rest; manifold; GJK; hinge/rope/6DOF; body-count perf | Phase 8 | done | `test_rt_game3d` covers Game3D contact accessors/fallbacks; `test_rt_physics3d` covers solver-iteration clamps/effect, AABB manifold count >1, rotated-box clipped OBB manifold count >1, island-batched contact scheduling with `PHYSICS_ISLAND_BATCH_TARGET` (`bodies=257 piles=32 islands=32`), convex-hull GJK/EPA hull/sphere/capsule/box coverage with separated-overlapping-AABB rejection and `PHYSICS_CONVEX_GJK_TARGET` (`pairs=32 contacts=32`), sparse 321-body step stress, mesh narrow-phase BVH build/use for sphere, box, and convex-hull contacts with `PHYSICS_MESH_BVH_TARGET` (`built_mesh_bvhs=1` across 16 tiles), hinge/rope/SixDof limits, SixDof joint-frame pose-angle clamping, locked-axis stability, and linear-motor coexistence; `test_graphics3d_abi_surface` guards names |  |
| TEST-9 | Navigation | Autogen equivalence; tile rebuild; avoidance bounds; agent perf | Phase 9 | done | `test_rt_navagent3d` covers avoidance property clamps, RVO lateral passing, head-on deadlock avoidance, multi-pair crowd crossing, grid-vs-full-scan parity, and the 200-agent `NAVAGENT_CROWD_TARGET`; `test_rt_navmesh_blend` covers scene voxel `Bake`, `BakeTiled`, retained source `RebuildTile` geometry refresh, off-mesh link bridging, directed traversal, endpoint validation, count telemetry, agent-radius portal gating, `AddObstacle`/`UpdateObstacle`/`RemoveObstacle` tile carving, exact triangle-footprint carving, polygon area/cost metadata, weighted `LastPathCost`, and off-mesh link kind/cost/state metadata; `test_rt_game3d` and `g3d_openworld_slice_probe` verify world-scoped nav bake sees resident streamed-terrain source nodes; ABI/link guards cover public surface | clipped sub-polygon carving remains a future refinement |
| TEST-10 | Animation | IK error bounds; additive; retarget; anim-LOD | Phase 10 | partial | `test_rt_animcontroller3d` covers `IKSolver3D` two-bone target error/weight blending/clearing, look-at local +Z aiming, FABRIK chain bounds, true additive bind-pose delta composition versus replace overlay, `CrossfadeLayerAdditive` blending, deterministic `SetAnimationLOD` throttling, and `SetBlendTree` controller base-pose driving; `test_rt_game3d` covers `Animator3D.playLayerAdditive`, `crossfadeLayerAdditive`, event capture, `Animator3D.setBlendTree`, and `Animator3D.setIKSolver`; `test_rt_navmesh_blend` covers `BlendTree3D` 1D/2D weights and bad-handle rejection; `test_rt_skeleton3d` covers `Animation3D.Retarget` name-based mapping, metadata preservation, and bad handles; `g3d_openworld_slice_probe` covers committed imported skinned play/crossfade plus LookAt IK binding and terrain-sampled TwoBone foot IK; ABI/link guards cover public methods | humanoid/proportional retarget mapping, bone-count LOD, and terrain-foot orientation/visible-character polish remain |
| TEST-11 | Asset pipeline | Compressed tolerance per backend; KTX2/precompressed blocks; glTF camera/scene | Phase 11 | partial | `test_rt_canvas3d` covers `TextureAsset3D.LoadKTX2` RGBA8 metadata/per-mip fallback switching, material binding, already-bound material resolution after residency changes, compressed-only rejection, retained native BC7 mip payloads, native backend submission, native resident mip helper selection/pending-byte math, mip residency byte telemetry, BC3/BC7 modes 0-7 software decode, representative ETC2/ASTC block decode, ETC2/ASTC KTX2 `Pixels` fallbacks, and native-only ASTC draw forwarding; `test_vgfx3d_backend_utils` covers compressed block-row upload budgeting/pending-byte math; `test_rt_canvas3d_production` covers `bc7`/`astc`/`etc2` capability defaults; `test_rt_model3d` covers the `Model3D` scene-indexed API contract, scene-local glTF perspective/orthographic camera import, and secondary glTF scene instantiation; `g3d_openworld_slice_streaming_hitch_native_compressed_probe` records resident native upload, raw-vs-compressed RAM/VRAM reduction, and final-frame tolerance; ABI/link guards cover public names | Basis/Draco/meshopt covered by optional Phase 11b tests; broader cross-platform compressed-texture baselines remain |
| TEST-12 | Vertical slice | Smoke; deterministic replay; software baseline; perf baselines | Phase 12 | partial | `g3d_openworld_slice_probe` covers all-four-quadrant bounded stream traversal, stream inspection hooks, World3D entity/body/draw/visibility/stream counters, world-scoped navmesh bake including streamed terrain, rendered heightmapped terrain payload access and collider residency, visible KTX2/BC7 texture asset residency, skinned glTF play/crossfade plus LookAt IK binding, committed GLB/WAV fixture loads, visible terrain-sampled TwoBone foot IK markers, async model handle completion, character/physics/nav stepping, committed software final-frame baseline comparison, and deterministic replay; `g3d_openworld_slice_long_traversal` repeats all-quadrant residency churn and replay checks; `g3d_openworld_slice_visibility_dense_probe` records authored dense visibility reduction and no missing geometry; `g3d_openworld_slice_perf_probe` records deterministic software perf metrics; `g3d_openworld_slice_perf_harness` validates/normalizes perf metrics for CI; `g3d_openworld_slice_gpu_smoke` covers capability-gated GPU backend execution plus a degenerate-basis normal-map robustness pass; Release macOS software/Metal perf baselines are recorded; package dry-run covers sample asset layout | Windows/Linux perf baselines remain |

## Fixtures

| ID | Fixture | Source | Status | Proof / link | Notes |
|---|---|---|---|---|---|
| FIX-001 | 10k-node scene (cull scaling) | Phase 0 | done | `test_rt_scene3d` generated 100x100 drawable-node grid | covers Scene3D cull/query candidate scaling; Release open-world perf lane now records named local wall-clock timing |
| FIX-002 | >4 km² terrain/world stand-in (streaming) | Phase 0/5 | partial | `examples/3d/openworld_slice/assets/world/terrain.vscn` declares four far-apart 65x65 tiles at 34m scale with committed height sidecars and CTest stream-center swaps; resident tiles render as terrain meshes, spawn matching heightfield collider entities, contribute hidden nav-bake source nodes, and repeat all-quadrant churn in `long_traversal.zia` over staged stream ticks; `test_rt_game3d` adds the explicit stitched-seam >4096-unit / >4 km2 proof; local software/Metal traversal proof records 18,939,904 m2, max stream bytes 327730, zero pending requests, and replay checksum | streamed nav bake/export tooling remains |
| FIX-003 | Dense city/forest (occlusion/LOD) | Phase 6 | done | `test_rt_canvas3d` generated dense covered-draw fixture; `test_rt_scene3d` generated indexed/PVS visibility fixtures; `g3d_openworld_slice_visibility_dense_probe` authored city/forest scene and Release baseline | records 169 drawables to 49 submitted draws, 50.407% fill-proxy reduction, and no missing software pixels |
| FIX-004 | Many-light scene (clustered/CSM) | Phase 7 | done | `test_rt_canvas3d` generated 24-light clustered/software fixture; `g3d_openworld_slice_gpu_smoke` GPU 24-light clustered fixture plus authored 3-cascade CSM fixture and Release baseline |  |
| FIX-005 | Body-count stress (physics) | Phase 8 | done | `test_rt_physics3d` sparse 321-body step stress, >128 contact/event storage growth, `PHYSICS_ISLAND_BATCH_TARGET` 257-body / 32-pile island-batched resting fixture, `PHYSICS_MESH_BVH_TARGET` 16-tile mesh broadphase/BVH fixture, `PHYSICS_CONVEX_GJK_TARGET` 32-pair mixed convex fixture, and SixDof pose-angle stability fixtures |  |
| FIX-006 | Agent-count nav scene | Phase 9 | done | `test_rt_navagent3d` `NAVAGENT_CROWD_TARGET: agents=200 frames=180 update_us=564686 min_pair_distance=1.142 crossed=170` | Release perf build baseline recorded locally |
| FIX-007 | Committed skinned character + GLB/glTF-deps + audio | CO-9 | done | `examples/3d/openworld_slice/assets/models/skinned_agent.gltf` plus `skinned_agent.bin`, `assets/models/triangle.glb`, and `assets/audio/jump.wav`; `g3d_openworld_slice_probe`; `g3d_openworld_slice_package_dry_run` | Fixtures are synthetic/redistributable or copied from existing repository assets. |
| FIX-008 | Compressed-texture / KTX2/precompressed assets | Phase 11 | partial | `test_rt_canvas3d` writes minimal KTX2 RGBA8, BC7, ETC2, and ASTC fixtures at test time; `examples/3d/openworld_slice/assets/textures/` commits tiny RGBA8 and BC7 KTX2 fixtures for sample/package coverage | broader texture corpus, native compressed upload fixtures, and Phase 11b fixtures remain |

## Docs

| ID | Page | Source | Status | Proof / link | Notes |
|---|---|---|---|---|---|
| DOC-001 | Concurrency + determinism model | Phase 1 | done | `docs/viperlib/graphics/game3d.md` worker-control section; `docs/graphics3d-architecture.md` threading model documents copied-data worker jobs, deterministic ordered merge, main-thread-only Graphics3D/Game3D handle mutation, the commit-queue boundary, and the focused TSan lane script | Future asset decode/upload docs reopen under DOC-010 / Phase 4 |
| DOC-002 | Large worlds: floating origin + streaming + partition | Phase 2/5 | partial | `docs/viperlib/graphics/game3d.md` covers floating-origin API, `World3D.stream`, `World3D` aggregate telemetry counters, `WorldStream3D` cell and terrain manifest schemas including height sidecars and authored material/collision/nav metadata, staged `update(dt)` load budgeting and `pendingRequestCount`, rendered terrain payload/collider residency telemetry, stitched adjacent terrain tile seams, and the `openworld_slice` CTest smoke | broader perf guidance still todo |
| DOC-003 | Visibility + lighting scaling | Phase 6/7 | done | `docs/viperlib/graphics/rendering3d.md` covers Scene3D indexed visibility/query behavior, occlusion controls, LOD/impostors, clustered-light gates, the software and GPU many-light baselines, and cascaded shadow gates/runtime behavior; `docs/graphics3d-architecture.md` covers the open-world visibility and GPU smoke probes |  |
| DOC-004 | Physics depth (joints, solver, convex) | Phase 8 | done | `docs/viperlib/graphics/physics3d.md` covers solver iterations, island-batched contact scheduling telemetry, BVH-backed triangle-mesh candidate pruning including convex-hull contacts, convex-hull GJK/EPA hull/sphere/capsule/box behavior, sleeping controls, sparse body-count stress, hinge/rope/SixDof limits, and SixDof pose-angle semantics |  |
| DOC-010 | Async assets | Phase 4 | partial | `docs/viperlib/graphics/game3d.md` covers `Assets3D.LoadModel*Async`, `AssetHandle3D` worker-scheduled/cancel/error behavior including missing-path, unsupported-extension, malformed glTF JSON failures, corrupt required texture payload failures, glTF/GLB root plus external/embedded buffer/image staging, worker-side PNG/BMP/JPEG/GIF image decode to raw RGBA POD plus bounded commit-side `Pixels` preparation, worker-side static/skinned/morph-target glTF triangle-list/strip/fan mesh decode to raw `Mesh3D`/`MorphTarget3D` POD with commit-side normal regeneration, skin remapping, and morph attachment, required-buffer/accessor-range/corrupt-texture preload validation, background filesystem/package-aware `Preload` / `PreloadAsset` through world-drained commits, `SetUploadBudget` decoded-image commit-slice gating, `SetResidencyBudget` texture-pixel accounting, `SetResidencyHint` priority/distance template-cache eviction, `GetResidentBytes` resident-byte cache telemetry, `ClearCache` generation guarding, and `Evict`; `examples/3d/openworld_slice/README.md` documents the streaming hitch probe; `docs/graphics3d-guide.md` and `docs/viperlib/graphics/rendering3d.md` document `Canvas3D.SetTextureUploadBudget`, `TextureUploadBytes`, `TextureUploadPendingBytes`, `Mesh3D.Resident`, and `SceneNode3D.SetLodResident`; `runtime-changes.md` calls out the named native-compressed hitch proof and local resident-byte/backend pending-byte proof | native-compressed hitch docs are current |
| DOC-005 | Navigation + AI depth | Phase 9 | done | `docs/graphics3d-guide.md` and `docs/viperlib/graphics/rendering3d.md` document `NavMesh3D.Bake`, `BakeTiled`, retained-source `RebuildTile`, `AddOffMeshLink`/`OffMeshLinkCount`, link metadata APIs, `AddObstacle`/`UpdateObstacle`/`RemoveObstacle`/`ObstacleCount`, exact triangle-footprint carving, polygon area/cost metadata, `LastPathCost`, the `agentRadius` portal gate, and `NavAgent3D` RVO-style avoidance controls; `docs/viperlib/graphics/game3d.md` documents `World3D.bakeNavMesh` / `bakeTiledNavMesh` and resident streamed-terrain bake source participation | clipped sub-polygon carving remains a future refinement |
| DOC-006 | Animation depth (IK, additive, blend trees, retarget) | Phase 10 | partial | `docs/graphics3d-guide.md` and `docs/viperlib/graphics/rendering3d.md` document `IKSolver3D`, `AnimController3D.SetIKSolver`, `AnimController3D.PlayLayerAdditive`, `CrossfadeLayerAdditive`, `SetAnimationLOD`, `SetBlendTree`, `BlendTree3D`, and `Animation3D.Retarget`; `docs/viperlib/graphics/game3d.md` documents `Animator3D.playLayerAdditive`, `crossfadeLayerAdditive`, `setBlendTree`, `setIKSolver`, and the open-world terrain-foot IK probe | humanoid/proportional retarget mapping, terrain-foot orientation guidance, and deeper animation LOD docs remain |
| DOC-007 | Asset pipeline (compression, KTX2, glTF camera/scene) | Phase 11 | partial | `docs/graphics3d-guide.md` documents `TextureAsset3D`, mip residency telemetry, active RGBA8 mip fallback binding with material draw-time resolution, retained native compressed mip payloads, backend compression gates, supported BC3/BC7/ETC2/ASTC software fallback scope, resident native upload telemetry, raw-vs-compressed RAM/VRAM reduction proof, `Model3D` scene-indexed APIs, scene-local glTF cameras, and secondary glTF scenes; `docs/viperlib/graphics/rendering3d.md` covers material/capability behavior, texture residency telemetry, compressed fallback/native-only behavior, and the Model3D camera/scene contract | broader cross-platform compressed-texture baselines remain |
| DOC-008 | Update `docs/graphics3d-architecture.md` + `docs/viperlib/graphics/` pages | Phase 12 | todo |  |  |
| DOC-009 | Every page has a ctest-compiled snippet | All | todo |  |  |

## Sample

| ID | Item | Source | Status | Proof / link | Notes |
|---|---|---|---|---|---|
| SLICE-001 | `examples/3d/openworld_slice/` full-stack sample | Phase 12 | partial | `g3d_openworld_slice_probe`; `g3d_openworld_slice_perf_probe`; `g3d_openworld_slice_perf_harness`; `g3d_openworld_slice_long_traversal`; `g3d_openworld_slice_gpu_smoke`; `g3d_openworld_slice_package_dry_run` | streams/renders/simulates/replays in software across all four stream quadrants with bounded cell/tile/collider/nav-source residency, repeated deterministic residency churn, visible heightmapped terrain payloads, streamed-terrain world nav bake inclusion, visible KTX2/BC7 `TextureAsset3D` material/residency fixtures, imported skinned play/crossfade plus LookAt IK binding, committed GLB/WAV fixture load coverage, visible terrain-sampled TwoBone foot IK markers, a committed software visual baseline, deterministic perf probe with reusable harness parsing, named local macOS Release software/Metal perf baseline, and capability-gated GPU smoke with a degenerate-basis normal-map pass; native-compressed hitch proof is covered locally |
| SLICE-002 | Editor runtime hooks consumed by a probe | Phase 12 | partial | `test_rt_game3d`; `test_rt_canvas3d`; `g3d_openworld_slice_probe`; `g3d_openworld_slice_perf_probe` | `WorldStream3D` cell/tile entry inspection, including terrain heightmap sidecar paths, `World3D` aggregate perf/editor counters, and world-scoped navmesh bake trigger with streamed terrain landed; cross-platform perf capture remains; editor UI stays in ViperIDE roadmap |
| SLICE-003 | Deterministic replay + software visual baseline | Phase 12 | done | `g3d_openworld_slice_probe`; `examples/3d/openworld_slice/assets/baselines/openworld_slice_software.png` | named-hardware perf stays under `P12-004` |
