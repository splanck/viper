# Runtime contract tracker

Tracks every runtime contract from `../runtime-changes.md`. Most new rows start
`todo`; carryover rows may reflect verified current status.

## 0. Carryover runtime items (Phase C)

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-CARRY-001 | Render-target finalization or `BackendSupports("rt-finalize")` | §0 / CO-4 | done | existing `FinalizeFrame`/`ScreenshotFinal` contract | `test_rt_canvas3d_gpu_paths`; `test_runtime_surface_cli`; graphics3d non-display lane | `api-spec.md` | local 2026-05-29 hardening pass | Backend capability probes remain per platform |
| R-CARRY-002 | Lifetime/diagnostics (`Game3D.<Type>.<method>`) + leak probes | §0 / CO-3 | done | destroyed world/entity and double-despawn diagnostics include the failing API; missing package assets return terminal error handles; quality fallback reasons remain inspectable; imported groups despawn repeatedly without registry retention; cache clear/reload remains usable | `test_rt_game3d`; `g3d_test_game3d_destroyed_handle_reject`; `g3d_test_game3d_destroyed_entity_reject`; `g3d_test_game3d_double_despawn_reject`; `g3d_test_game3d_assets_probe`; `g3d_test_canvas3d_quality_profiles` | `docs/viperlib/graphics/game3d.md`; `runtime-changes.md` | focused ctest slice green | R-LIFE-* |
| R-CARRY-003 | `Light3D.CastsShadows` + material texture-presence getters | §0 / CO-6 | done | `Light3D.CastsShadows` public get/set, persisted VSCN flag, glTF imported-light default; `Material3D.HasTexture/HasNormalMap/HasSpecularMap/HasEmissiveMap/HasMetallicRoughnessMap/HasAmbientOcclusionMap/HasEnvMap` | Canvas3D accessor coverage + GPU shadow-selection + Scene3D roundtrip + ABI tests | `docs/viperlib/graphics/rendering3d.md`; `api-spec.md` | focused ctest slices green; `./scripts/check_runtime_completeness.sh` |  |
| R-CARRY-004 | Implicit-lighting decision + conditional gating/count | §0 / CO-5 | done | no implicit fallback lights; explicit `SetDefaultLighting()` path only | `test_rt_canvas3d_gpu_paths` explicit-dark regression | `docs/viperlib/graphics/rendering3d.md`; `api-spec.md`; `02-decisions.md` | `ctest --test-dir build -R '^test_rt_canvas3d_gpu_paths$' --output-on-failure` | D-007 closed |
| R-CARRY-005 | Metal robustness guards + probe | §0 / CO-7 | done | Metal skybox shader safe-normalizes zero camera vectors to Canvas3D's `-Z` convention; Canvas3D queues normal-mapped degenerate-basis meshes through a finite tangent snapshot while backend shaders retain safe normal fallbacks | `test_rt_canvas3d_gpu_paths`; `test_vgfx3d_backend_metal_shared`; `g3d_openworld_slice_gpu_smoke` | `docs/graphics3d-architecture.md`; `docs/graphics3d-guide.md`; `metal.md` | focused ctest slice green; local `VIPER_3D_BACKEND=metal ... gpu_smoke.zia` passed | R-METAL-006 closed |

## ABI / class registration

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-ABI-001 | Append class ids + `RT_CLASS_BEGIN` for every new public 3D class | `runtime-changes.md` intro, `api-spec.md` conventions | partial | ABI guard covers appended `AssetHandle3D`, `WorldStream3D`, `TextureAsset3D`, Hinge/Rope/SixDof, and prior Game3D additions | `test_graphics3d_abi_surface` | `progress/04-api-surface.md` | focused ctest slice green | keep updating for every future public class; never renumber existing `RT_G3D_*_CLASS_ID` values |

## 1. Concurrency runtime

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-JOB-001 | Worker pool + `rt_job_submit`/`wait`/parallel-for on `rt_platform.h` | §1 | done | Existing `Viper.Threads.Pool` / `Viper.Threads.Parallel` runtime is the approved worker substrate; `World3D` owns a lazy internal `ThreadPool` and dispatches animator batches through it | `g3d_3dnext2_surface_probe`; `test_rt_game3d`; `test_rt_parallel`; `test_rt_parallel_reduce`; `test_rt_threadpool`; `test_rt_concurrency`; `test_rt_gltf`; `g3d_openworld_slice_streaming_hitch_probe` | `docs/viperlib/graphics/game3d.md`; `docs/viperlib/threads.md`; `api-spec.md`; `../../3dnextlevel3/tsan-concurrency-lane.md`; `../../3dnextlevel3/determinism-closure.md` | `scripts/g3d_tsan_concurrency_lane.sh --no-configure` passed 9/9 under ThreadSanitizer on Apple M4 Max; focused NL3-031 determinism lane passed locally | Phase-1 race-stress and VM/native determinism acceptance are closed locally |
| R-JOB-002 | Deterministic ordered-merge primitive | §1 | done | `rt_parallel_map` preserves input order for worker-produced results; Game3D worker batches merge before visible state advances | `g3d_3dnext2_surface_probe`; `test_rt_parallel`; `test_rt_game3d`; `test_crosslayer_arith`; native-run Zia promise tests | `docs/viperlib/threads.md`; `docs/graphics3d-architecture.md`; `../../3dnextlevel3/determinism-closure.md` | `ctest --test-dir build -R '^(g3d_3dnext2_surface_probe\|test_rt_parallel\|test_rt_game3d)$' --output-on-failure`; focused NL3-031 determinism lane passed locally | Future simulation-touching changes must rerun parity and ADR proof when IL/VM/native semantics change |
| R-JOB-003 | Main-thread commit/upload queue | §1 | done | Internal Graphics3D commit queue accepts worker-produced callbacks, drains FIFO on the main thread with item and decoded-byte cost budgets, and exposes pending/submitted/drained telemetry for internal tests | `test_rt_g3d_commit_queue` | `docs/graphics3d-architecture.md` | `ctest --test-dir build -R '^test_rt_g3d_commit_queue$' --output-on-failure` | §4 now routes async glTF payload commits through it; sub-mip block-row upload-slice budgeting (`vgfx3d_upload_block_rows_for_budget`) now has a monotonic-drain proof in `test_vgfx3d_backend_utils`; wiring it into the GPU native-upload loops is Tier-C (on-device verification) |
| R-JOB-004 | Thread-safe allocator/GC/handle paths | §1 | done | Existing thread/parallel tests cover pool ownership, nested calls, retained map results, task traps, and shutdown; Phase 1 3D worker paths are copied-data jobs plus commit-queue enqueue, while public handle mutation and commit draining remain main-thread-only | `test_rt_threadpool`; `test_rt_parallel`; `test_rt_parallel_reduce`; `g3d_3dnext2_surface_probe`; `test_rt_g3d_commit_queue` worker-drain rejection; `scripts/g3d_tsan_concurrency_lane.sh` | `docs/viperlib/threads.md`; `docs/graphics3d-architecture.md`; `runtime-changes.md`; `../../3dnextlevel3/tsan-concurrency-lane.md` | `scripts/g3d_tsan_concurrency_lane.sh --no-configure` passed 9/9 under ThreadSanitizer on Apple M4 Max | Current Phase 4 worker-backed asset decode keeps GC/backend mutation on the main-thread commit path; future worker-touched GC/backend paths reopen this audit |
| R-JOB-005 | `World3D.workerCount` / `jobsEnabled` / `setWorkerCount` controls | §1 | done | `rt_game3d_world_get_worker_count`; `rt_game3d_world_get_jobs_enabled`; `rt_game3d_world_set_worker_count` | `test_rt_game3d`; `test_graphics3d_abi_surface` | `docs/viperlib/graphics/game3d.md` | focused ctest slice green | internal-first; Game3D lower/camel style |

## 2. Floating origin

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-ORIG-001 | Rebase hooks (nodes/bodies/audio/particles/queries) | §2 | done | `Scene3D.RebaseOrigin` shifts root-level scene subtrees; `Physics3DWorld.RebaseOrigin` shifts bodies plus live/previous contact/query state; `Particles3D.RebaseOrigin` and `Sprite3D.RebaseOrigin` cover standalone generated paths; `World3D.rebaseOrigin` applies the same between-frame cross-system boundary as automatic floating-origin rebases for scene, physics, camera, explicit listener, and world-owned effects | `test_rt_scene3d`, `test_rt_game3d`, `test_rt_physics3d`, `test_rt_canvas3d`, `test_rt_particles3d_contract`, `test_graphics3d_abi_surface` | `docs/viperlib/graphics/rendering3d.md`, `docs/viperlib/graphics/game3d.md`, `docs/viperlib/graphics/physics3d.md` | focused ctest slice green | existing query/event objects remain pre-rebase snapshots; callers re-query after the boundary |
| R-ORIG-002 | Promote cull bounds + physics state precision | §2 | partial | Scene3D transformed spatial/query/draw-candidate bounds are double precision; Physics3D body position/velocity integration keeps double precision at far origin; matrix-backed and identity-raw/generated floating-origin render upload subtracts camera origin before float narrowing | `test_rt_scene3d`; `test_rt_physics3d`; `test_rt_canvas3d` | `docs/viperlib/graphics/rendering3d.md`; `docs/viperlib/graphics/physics3d.md`; `runtime-changes.md`; `api-spec.md` | focused ctest slice green | exact backend frustum tests and caller-supplied float instancing inputs remain float precision |
| R-ORIG-003 | Camera-relative upload path | §2 | done | Canvas3D internal camera-relative upload stores the active camera origin per frame, zeroes camera frame position/view translation, rebases double `DrawMesh` model translations, identity-matrix raw `Mesh3D.AddVertex` geometry, standalone `Particles3D` / `Sprite3D` generated vertices, and point/spot light positions; Game3D enables it from `World3D.floatingOrigin` | `test_rt_canvas3d`; `test_rt_game3d` | `docs/viperlib/graphics/rendering3d.md`; `docs/viperlib/graphics/game3d.md`; `runtime-changes.md`; `api-spec.md` | focused ctest slice green | GPU upload remains float; rendered near/far parity and per-cell policy are tracked separately |
| R-ORIG-004 | `World3D.floatingOrigin` capability gate | §2 | done | `rt_game3d_world_get/set_floating_origin`; `worldOrigin`; `setOriginRebaseThreshold` | `test_rt_game3d` | `docs/viperlib/graphics/game3d.md` | focused ctest slice green | off = unchanged |

## 3. Spatial index

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-IDX-001 | Index over scene graph, incremental refit | §3 | partial | internal Scene3D BVH over visible drawable nodes; transform-only dirties refit the tree, topology/material-shape dirties rebuild lazily; generated 10k grid proves BVH shape and query/draw candidate reduction | `test_rt_scene3d` | `docs/viperlib/graphics/rendering3d.md` | focused ctest slice green | named-hardware timing baseline still todo |
| R-IDX-002 | `Scene3D.QueryAABB` / `QuerySphere` / `RaycastNodes` | §3 | partial | public query methods use indexed candidates with deterministic flat-walk fallback; `VisibleNodeCount` draw telemetry; Physics3D keeps a documented sibling broadphase for body-centric solver needs | `test_rt_scene3d`; `test_rt_physics3d`; `test_graphics3d_abi_surface` | `docs/viperlib/graphics/rendering3d.md`; `docs/viperlib/graphics/physics3d.md`; `api-spec.md`; `../../3dnextlevel3/spatial-physics-broadphase-proof.md` | focused ctest slice green | PascalCase public methods |
| R-IDX-003 | Replace draw walk with index query (flag-gated) | §3 | partial | Scene3D.Draw uses indexed candidates before exact selected-LOD/impostor frustum culling; internal `use_spatial_index` flag keeps the flat path for parity; generated 10k fixture keeps candidates below 10% of drawables | `test_rt_scene3d` | `docs/viperlib/graphics/rendering3d.md` | focused ctest slice green | release perf-lane timing still todo |
| R-IDX-004 | Shared query contract; physics broadphase may be sibling | §3 | done | Scene query semantics are shared and tested; physics broadphase remains separate by documented proof because it is body-centric and solver-facing | `test_rt_scene3d`; `test_rt_physics3d` | `api-spec.md`; `docs/viperlib/graphics/physics3d.md`; `../../3dnextlevel3/spatial-physics-broadphase-proof.md` | focused ctest slice green |  |

## 4. Async asset + upload

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-ASYNC-001 | Worker decode + budgeted main-thread upload | §4 | partial | Valid uncached `AssetHandle3D` glTF/GLB requests stage root bytes plus external, data URI, and bufferView-backed buffer/image payloads on a runtime worker, decode PNG, BMP, JPEG, and GIF image payloads into raw RGBA POD, prepare decoded RGBA POD into `Pixels` across bounded main-thread commit slices, and decode static/skinned/morph-target glTF triangle-list/strip/fan mesh primitives with positions, optional normals, sparse accessor overrides, JOINTS/WEIGHTS attributes, and morph deltas into raw `Mesh3D`/`MorphTarget3D` POD, validate required buffer payloads/accessor byte ranges/corrupt required texture images during preload, then build/decode runtime model/template/entity results through the §1 main-thread commit queue; `SetUploadBudget` gates commit drains by decoded image byte cost; blocking glTF loads reject corrupt required data-URI texture images instead of dropping material maps; missing mesh normals are regenerated during commit; template-cache residency estimates include decoded material texture pixels; non-glTF formats enqueue a main-thread load request; `g3d_openworld_slice_streaming_hitch_probe` records blocking-vs-async timing and proves zero-upload-budget pending behavior | `test_rt_game3d`; `test_rt_gltf`; `test_gif_decode`; `test_rt_g3d_commit_queue`; `g3d_openworld_slice_probe`; `g3d_openworld_slice_streaming_hitch_probe`; `test_runtime_surface_audit` | `docs/viperlib/graphics/game3d.md`; `runtime-changes.md`; `examples/3d/openworld_slice/README.md` | `ctest --test-dir build -R '^(test_rt_g3d_commit_queue|test_rt_game3d|test_rt_gltf|test_gif_decode|test_pixels|g3d_openworld_slice_streaming_hitch_probe|test_graphics3d_abi_surface|test_runtime_surface_audit|test_runtime_surface_cli)$' --output-on-failure` | Resident-byte churn, backend pending-byte baseline proof, and named native-compressed hitch rerun are covered locally |
| R-ASYNC-002 | Model + template async handles, residency, eviction | §4 | partial | `AssetHandle3D` wraps entity and template load requests; first observation services the commit queue and starts worker staging/loading for valid uncached requests, including worker-side PNG/BMP/JPEG/GIF-to-RGBA POD image decode for staged glTF image payloads, bounded commit-side `Pixels` preparation, and worker-side static/skinned/morph-target triangle-list/strip/fan/sparse-accessor mesh decode to `Mesh3D`/`MorphTarget3D` POD with JOINTS/WEIGHTS attributes and morph deltas; `Assets3D.Preload` and `Assets3D.PreloadAsset` start filesystem and package-aware template async paths immediately, and world ticks/simulation steps drain commits with the fixed queue item budget plus `SetUploadBudget` decoded-image byte budget; `cancel()` is terminal before worker completion and stable after completion; missing filesystem/asset paths, unsupported model extensions, malformed glTF JSON, missing required buffers, invalid accessor ranges, and corrupt required PNG/BMP/JPEG/GIF texture payloads complete with non-cancel load errors; `SetResidencyBudget`, `GetResidentBytes`, `SetResidencyHint`, and `Evict` evict cached templates without invalidating held handles and expose resident-byte telemetry, with decoded material texture pixels counted in residency bytes; `ClearCache` advances the cache generation so pre-clear preload jobs cannot repopulate the new cache | `test_rt_game3d`; `test_rt_gltf`; `test_gif_decode`; `test_graphics3d_abi_surface`; `test_rt_model3d`; `test_rt_graphics_surface_link`; `g3d_openworld_slice_probe` | `docs/viperlib/graphics/game3d.md`; `api-spec.md` | `ctest --test-dir build -R '^(test_rt_g3d_commit_queue|test_rt_game3d|test_rt_gltf|test_gif_decode|test_pixels|test_graphics3d_abi_surface|test_runtime_surface_audit|test_runtime_surface_cli)$' --output-on-failure` | named native-compressed hitch baseline is covered; full world-stream hitch proof remains |
| R-ASYNC-003 | Texture/mesh mip/LOD residency hooks | §4 | done | `TextureAsset3D.SetResidentMipRange` exposes mip residency byte telemetry and RGBA8/native source switching; `Material3D` retains accepted `TextureAsset3D` sources and resolves the current resident source at draw submission; `Mesh3D.Resident` / `ResidentBytes` and `SceneNode3D.SetLodResident` / `GetLodResidentBytes` expose mesh/LOD payload residency and let streaming cells remeasure resident bytes after detail demotion | `test_rt_canvas3d`; `test_rt_scene3d`; `test_rt_game3d`; `test_graphics3d_abi_surface`; `g3d_openworld_slice_probe` | `docs/graphics3d-guide.md`; `docs/viperlib/graphics/rendering3d.md`; `docs/viperlib/graphics/game3d.md`; `api-spec.md` | focused ctest slice green | native-compressed hitch proof is covered; RAM reduction proof remains outside this hook |
| R-ASYNC-004 | Backend texture upload budget + telemetry | §4 | done | `Canvas3D.SetTextureUploadBudget` gates Metal/OpenGL/D3D11 Pixels-backed 2D material texture, cubemap, and native-compressed mip uploads by backend-frame byte slices; `Canvas3D.TextureUploadPendingBytes` reports queued row/native bytes; `Canvas3D.TextureUploadBytes` reports latest-frame upload bytes; cache hits and software/unsupported paths report `0` | `test_rt_canvas3d`; `test_vgfx3d_backend_utils`; `test_graphics3d_abi_surface` | `docs/graphics3d-guide.md`; `docs/viperlib/graphics/rendering3d.md`; `api-spec.md` | focused ctest slice green | Backend pending-byte baseline and named native-compressed hitch proof are covered locally |
| R-ASYNC-005 | Template-cache resident-byte telemetry | §4 | done | `Assets3D.GetResidentBytes` returns the shared ModelTemplate cache resident-byte counter, saturates the unsigned internal counter into the public signed integer range, and returns `0` in disabled-graphics builds | `test_rt_game3d`; `test_graphics3d_abi_surface`; `g3d_openworld_slice_streaming_hitch_probe` | `docs/viperlib/graphics/game3d.md`; `runtime-changes.md`; `api-spec.md`; `examples/3d/openworld_slice/README.md` | focused ctest slice green | Resident-byte churn proof is covered; backend pending-byte baseline proof is covered locally; hardware GPU/process memory baselines remain backend-checklist work |

## 5. World partition + terrain streaming

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-STREAM-001 | Terrain tile grid + seam stitching (lift 4096² cap) | §5 | done | Resident `tiles[]` manifest entries instantiate `Terrain3D` payloads using manifest dimensions/scale, apply optional `viper-heightmap-v1` sidecars, render through `World3D.drawScene`, unload with stream residency, stitch full adjacent resident tile edges in world-height space, invalidate cached LOD meshes, and rebuild collider/nav sources from the stitched height grid | `test_rt_game3d`; `g3d_openworld_slice_probe`; `test_graphics3d_abi_surface`; `test_rt_graphics_surface_link` | `docs/viperlib/graphics/game3d.md`; `api-spec.md` | focused ctest slice green; 9600-unit / >4 km2 proof beyond one heightmap cap | NL3-011 closed locally |
| R-STREAM-002 | Scene cell load/unload (subtree+physics+nav) | §5 | partial | `World3D.stream` owns a lazy stream handle; `WorldStream3D.mountCells` parses manifests and loads/unloads resident `.vscn` scene subtrees by center/radii/budget; `update(dt)` stages desired loads behind a deterministic per-frame budget and exposes deferred work through `pendingRequestCount`; terrain stream residency now includes static heightfield physics bodies plus hidden mesh-only nav-bake source nodes; long traversal probe repeats deterministic all-quadrant churn | `test_rt_game3d`; `g3d_openworld_slice_probe`; `g3d_openworld_slice_long_traversal`; `test_graphics3d_abi_surface` | `docs/viperlib/graphics/game3d.md`; `api-spec.md` | focused ctest slice green | named hitch proof remains |
| R-STREAM-003 | VSCN streaming manifest/extension + bake hook | §5 | partial | cell manifest schema supports `cells[]`; tiled terrain manifests support `tiles[]` with `name`, `path`, optional `heightmap`, `center`, `radius`, `bytes`, `width`, `depth`, `scale`, authored `material`, layer/collision metadata, `navArea`, `traversalCost`, and optional `sidecar` / `binarySidecar` paths; resident tiles own rendered heightmapped `Terrain3D` payloads, heightfield collider entities, and hidden terrain nav-bake source nodes consumed by `World3D.bakeNavMesh` | `test_rt_game3d`; `g3d_openworld_slice_probe`; `test_graphics3d_abi_surface`; `test_rt_graphics_surface_link` | `docs/viperlib/graphics/game3d.md`; `api-spec.md` | focused ctest slice green | binary sidecar payloads now load into cell residency (`getCellSidecarBytes`); navmesh export/import landed (`NavMesh3D.Export`/`Import`); deeper streamed per-tile nav export remains |
| R-STREAM-004 | Per-tile heightfield colliders | §5 | partial | Resident terrain tiles build static heightfield bodies from the current terrain height grid, apply parsed collision layer/mask/enabled metadata, spawn invisible `<tile>_heightfield_collider` entities, spawn hidden `<tile>_navmesh_source` bake nodes, rebuild both after seam stitching, and remove those bodies/sources on unload/budget eviction | `test_rt_game3d`; `g3d_openworld_slice_probe` | `docs/viperlib/graphics/game3d.md`; `examples/3d/openworld_slice/README.md` | focused ctest slice green | streamed nav bake/export tooling remains |

## 6. Visibility

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-VIS-001 | Occlusion culling (software depth / portal/PVS) | §6 | done | conservative CPU coverage/depth grid for opaque draws; Scene3D feeds the grid from BVH draw candidates before Canvas3D sorting, while raw Canvas3D draws keep the sorted-queue fallback; authored visibility zones and directed portal links provide initial interior PVS; dense covered-draw fixture reduces 65 queued opaque submissions to one backend draw with 64 reported skips; indexed fixture narrows 130 drawables to 2 grid candidates; PVS fixture culls an unlinked room and reports `PvsCulledCount`; the authored city/forest probe records draw/fill-proxy reduction and no missing software pixels; `BackendSupports("occlusion")` reports baseline support | `test_rt_canvas3d`; `test_rt_scene3d`; `test_rt_canvas3d_gpu_paths`; `g3d_openworld_slice_visibility_dense_probe` | `docs/viperlib/graphics/rendering3d.md`; `api-spec.md`; `examples/3d/openworld_slice/baselines/perf_macos_apple_m4_max.md` | focused ctest slice green; local Release baseline recorded |  |
| R-VIS-002 | Authored-LOD selection + HLOD/impostors | §6 | partial | authored screen-error LOD selection; generated textured impostor proxy retained on scene nodes | `test_rt_scene3d` | `docs/viperlib/graphics/rendering3d.md`; `api-spec.md` | focused ctest slice green | auto mesh simplification/backend-baked HLOD remains stretch |
| R-VIS-003 | Reconcile `SetOcclusionCulling` alias | §6 | done | `SetFrustumCulling` is frustum-only; `SetOcclusionCulling` enables frustum + CPU occlusion | `test_rt_canvas3d`; `test_rt_canvas3d_production` | `docs/viperlib/graphics/rendering3d.md`; `api-spec.md` | focused ctest slice green | CO-8 closed for runtime/docs |

## 7. Lighting

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-LIT-001 | Clustered/forward+ light culling | §7 | done | `Canvas3D.SetClusteredLighting` and `MaxActiveLights` capability-gated; software backend provides a 64-light correctness baseline; real Metal/D3D11/OpenGL backend vtables advertise the bounded 64-light GPU upload/shader path; unsupported/fake backends stay on the 16-light forward cap | `test_rt_canvas3d`; `test_graphics3d_abi_surface`; `g3d_openworld_slice_gpu_smoke` | `docs/viperlib/graphics/rendering3d.md`; `api-spec.md`; `examples/3d/openworld_slice/baselines/perf_macos_apple_m4_max.md` | focused ctest slice green; local Metal Release baseline recorded | keep forward shaders; CSM remains separate |
| R-LIT-002 | Cascaded shadow maps + shadow-caster cap lift | §7 | done | shadow selector honors `Light3D.CastsShadows`; `Canvas3D.SetShadowCascades` capability gate keeps unsupported CSM explicit; four shadow slots support either selected directional casters or primary-light cascades with per-light split metadata | `test_rt_canvas3d`; `test_rt_canvas3d_gpu_paths`; `test_graphics3d_abi_surface`; `g3d_openworld_slice_gpu_smoke` | `docs/viperlib/graphics/rendering3d.md`; `api-spec.md`; `examples/3d/openworld_slice/baselines/perf_macos_apple_m4_max.md` | focused ctest slice green; local Metal Release baseline recorded |  |
| R-LIT-003 | Capability fallback to 16-light forward | §7 | done | unsupported clustered lighting reports no capability, traps on enable, keeps `MaxActiveLights == 16`, and the deferred draw payload caps at the forward limit; GPU smoke records the 16-light fallback before enabling the 64-light path | `test_rt_canvas3d`; `test_rt_canvas3d_gpu_paths`; `g3d_openworld_slice_gpu_smoke` | `docs/viperlib/graphics/rendering3d.md` | focused ctest slice green; local Metal Release baseline recorded |  |

## 8. Physics depth

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-PHYS-001 | Contact manifolds + iterated warm-started solver + Game3D contact accessors | §8 | partial | Game3D contact accessors added; `Physics3DWorld.SolverIterations` now iterates contacts and joints; AABB raw events and rotated box face contacts expose bounded multi-point manifolds | `test_rt_game3d`, `test_rt_physics3d`, `test_graphics3d_abi_surface` | `docs/viperlib/graphics/game3d.md`, `docs/viperlib/graphics/physics3d.md` | `Viper.Game3D.Collision3DEvent.contact*`; `Physics3DWorld.SolverIterations` | broader non-box manifold grouping remains |
| R-PHYS-002 | BVH/index mesh narrow-phase | §8 | done | per-mesh BVH prunes candidate triangles for mesh-vs-sphere/capsule/box/convex-hull contacts, mesh-vs-convex-hull runs GJK/EPA against candidate triangles, and the body-centric physics broadphase selects body pairs before per-mesh BVH traversal; full-scan fallback remains for BVH build/traversal failure | `test_rt_physics3d` | `docs/viperlib/graphics/physics3d.md` | `PHYSICS_MESH_BVH_TARGET` plus focused ctest slice green |  |
| R-PHYS-003 | GJK/EPA convex narrow phase | §8 | done | convex-hull pairs and hull-vs-simple primitive pairs use support-point GJK plus EPA penetration normal/depth; separated overlapping-AABB hulls and hull-vs-box pairs reject correctly, contained sphere contacts are detected, box/capsule/hull simple branches report finite normals and positive penetration, and a named 32-pair mixed convex target records coverage | `test_rt_physics3d` | `docs/viperlib/graphics/physics3d.md`; `api-spec.md` | `PHYSICS_CONVEX_GJK_TARGET` plus focused ctest slice green |  |
| R-PHYS-004 | Hinge/rope/6DOF joints | §8 | done | hinge anchor constraint, motor, angle readout, limits, rope max-length constraint, and SixDof frame-anchor, linear motor, linear limits, and joint-frame pose-angle limits registered as real `Viper.Graphics3D.*` classes | `test_rt_physics3d`, `test_graphics3d_abi_surface` | `docs/viperlib/graphics/physics3d.md`; `api-spec.md` | focused ctest slice green |  |
| R-PHYS-005 | Solver islands + scaled sleeping | §8 | done | Dynamic bodies expose `CanSleep`/`Sleeping`, manual `Sleep`/`Wake`, idle auto-sleep, and skip integration while sleeping; post-solve sleep islands freeze settled stacks; awake non-trigger contacts are scheduled through solver island batches with `LastSolverIslandCount`, `LastSolverActiveBodyCount`, and `LastSolverContactCount`; sparse 321-body step stress and a named 257-body / 32-pile resting target exercise body-count scaling | `test_rt_physics3d` | `docs/viperlib/graphics/physics3d.md` | `PHYSICS_ISLAND_BATCH_TARGET` plus focused ctest slice green | future tuning only if larger-world evidence exposes a threshold regression |

## 9. Navigation depth

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-NAV-001 | Navmesh auto-generation (voxelize→mesh) | §9 | done | `NavMesh3D.Bake` validates a `Scene3D`, gathers all `Mesh3D`-bearing nodes through world transforms, voxelizes geometry into an XZ heightfield, filters slope-walkable top surfaces, erodes by `agentRadius`, emits a shared-corner grid mesh decoupled from input density, and keeps `Build(mesh)` as the direct-triangle passthrough; `World3D.bakeNavMesh`/`bakeTiledNavMesh` expose the same path for Game3D/editor workflows and include resident streamed-terrain nav source nodes | `test_rt_navmesh_blend`; `test_rt_game3d`; `test_graphics3d_abi_surface`; `test_rt_graphics_surface_link`; `g3d_openworld_slice_probe` | docs updated | focused ctest slice green | region/watershed partitioning is a future refinement |
| R-NAV-002 | Tiled/streamable navmesh + tile rebuild + carving | §9 | partial | `NavMesh3D.BakeTiled` records tile size and retains voxel-cell source height/walkability plus corner mappings; `RebuildTile` refreshes one tile's geometry, heights, blocked state, adjacency, query grid, and off-mesh endpoint resolution from that retained source; tiled obstacle edits re-carve affected tiles, while non-tiled meshes still whole-mesh refilter; carving tests exact triangle-footprint overlap against obstacle volumes; all validate finite AABB bounds | `test_rt_navmesh_blend`; `test_graphics3d_abi_surface`; `test_rt_graphics_surface_link` | docs updated | focused ctest slice green | clipped sub-polygon carving remains |
| R-NAV-003 | Off-mesh links + `agent_radius` corridors | §9 | partial | `NavMesh3D` retains authored off-mesh links, refreshes endpoint triangle resolution after slope refilters, lets A* traverse directed/bidirectional link edges with link kind/cost/state metadata, supports polygon area names and traversal costs, reports weighted `LastPathCost`, and gates shared portals by `agent_radius * 2` | `test_rt_navmesh_blend`; `test_graphics3d_abi_surface`; `test_rt_graphics_surface_link` | docs updated | focused ctest slice green | no full polygon/corridor erosion or traversal animation/state-machine integration yet |
| R-NAV-004 | Local avoidance + pathfinding acceleration | §9 | done | `rt_navagent3d_update` solves opt-in same-NavMesh reciprocal velocity-obstacle avoidance (radius clamps to `>=0`) by scanning peers through a spatial agent grid (O(N) cell neighborhood, full-scan fallback), predicting collision risk over a bounded horizon, and selecting a deterministic speed-preserving candidate velocity; point location uses a uniform XZ query grid (`find_tri`, parity-tested) | `test_rt_navagent3d`; `test_graphics3d_abi_surface`; `test_rt_graphics_surface_link` | docs updated | `NAVAGENT_CROWD_TARGET: agents=200 frames=180 update_us=564686 min_pair_distance=1.142 crossed=170`; focused ctest slice green | path caching and time-sliced/hierarchical A* remain future refinements |

## 10. Animation depth

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-ANIM-001 | IK: two-bone + look-at + FABRIK | §10 | done | `IKSolver3D` owns same-skeleton IK state, validates parented two-bone/FABRIK chains, clamps finite weights, supports `SetPole` bend-plane control and `SetGroundNormal` terrain/contact orientation, and can apply controller-bound results after overlay composition and before palette generation; `LookAt` aims local +Z; openworld covers imported skinned play/crossfade plus LookAt IK and visible terrain-sampled TwoBone foot IK markers as sample proof | `test_rt_animcontroller3d`; `test_rt_game3d`; `g3d_openworld_slice_probe`; `g3d_test_graphics3d_docs_snippets`; ABI/link guards | docs updated | focused ctest slices green |  |
| R-ANIM-002 | True additive layers | §10 | partial | `AnimController3D.PlayLayerAdditive` and `CrossfadeLayerAdditive` compose `(overlay - bind_pose) * weight` onto the current base pose for masked overlay layers; Game3D additive wrappers forward and capture events | `test_rt_animcontroller3d`; `test_rt_game3d`; `g3d_openworld_slice_probe`; ABI/link guards | docs updated | focused ctest slices green | authored additive visual samples remain |
| R-ANIM-003 | Blend trees / blendspaces | §10 | partial | `BlendTree3D` owns an internal `AnimBlend3D`, computes normalized 1D/2D sample weights from finite parameters, can be passed directly to `Canvas3D.DrawMeshBlended`, and can drive `AnimController3D`/`Game3D.Animator3D` base poses | `test_rt_navmesh_blend`; `test_rt_animcontroller3d`; `test_rt_game3d`; ABI/link guards | docs updated | focused ctest slices green | higher-order blend-tree nodes remain |
| R-ANIM-004 | Retargeting + animation LOD | §10 | done | `AnimController3D.SetAnimationLOD` batches positive-dt updates at a requested lower rate while preserving accumulated playback time; `AnimController3D.SetBoneLod(maxBones)` freezes bones at/after the retained prefix for distant-character bone-count LOD; `Animation3D.Retarget` preserves clip metadata, maps channels by exact bone name before humanoid role aliases and index fallback, and scales keyed translations by bind-length ratio | `test_rt_animcontroller3d`; `test_rt_skeleton3d`; `g3d_test_graphics3d_docs_snippets`; ABI/link guards | docs updated | focused ctest slices green |  |

## 11. Asset pipeline depth

| ID | Item | Source | Status | Impl | Tests | Docs | Proof / link | Notes |
|---|---|---|---|---|---|---|---|---|
| R-TEX-001 | GPU texture compression upload + software decode | §11 | partial | `BackendSupports("bc7"/"astc"/"etc2")` names and capability bits are registered and capability-gated per backend/device; Metal/OpenGL/D3D11 submit resident native mip blocks under Canvas3D texture-upload budgets; RGBA8 KTX2 per-mip fallbacks provide CPU material binding; BC3 (DXT5), BC7 modes 0-7, representative ETC2 RGBA8/EAC individual/differential blocks, and ASTC LDR void-extent blocks have software reference decode to RGBA8; unsupported ETC2/ASTC block modes stay native-only with native payloads retained | `test_rt_canvas3d`; `test_rt_canvas3d_production`; `test_graphics3d_abi_surface`; `g3d_openworld_slice_streaming_hitch_native_compressed_probe` | `docs/graphics3d-guide.md`; `docs/viperlib/graphics/rendering3d.md` | focused ctest slice green plus native hitch CTest green | Basis/Draco/meshopt and broader cross-platform compressed-texture baselines remain |
| R-TEX-002 | KTX2/precompressed block loading + streaming mips | §11 | partial | `TextureAsset3D.LoadKTX2` and `LoadKTX2Asset` parse KTX2 headers, expose dimensions/mips/format/compressed flags, track declared mip payload bytes via `ResidentMipStart`/`ResidentMipCount`/`ResidentBytes`, retain native compressed mip payloads, select the active fallback/native source from the resident mip range, and let already-bound materials resolve the current asset source during draw submission | `test_rt_canvas3d`; `test_graphics3d_abi_surface`; `test_rt_graphics_surface_link`; `g3d_openworld_slice_streaming_hitch_native_compressed_probe` | `docs/graphics3d-guide.md`; `docs/viperlib/graphics/rendering3d.md`; `api-spec.md` | focused ctest slice green plus native hitch CTest green | broader cross-platform compressed-texture baselines remain |
| R-TEX-003 | glTF camera + immutable multi-scene queries/instantiation | §11 | partial | `Model3D` exposes immutable scene-indexed query/instantiate APIs over active/default and secondary glTF scenes, retaining scene-local `Camera3D` handles: `SceneCount`, `GetCameraCount`, `GetCamera`, `GetSceneName`, `InstantiateSceneAt` | `test_rt_model3d`, `test_graphics3d_abi_surface`, `test_rt_graphics_surface_link` | `docs/graphics3d-guide.md`; `docs/viperlib/graphics/rendering3d.md`; `api-spec.md` | focused ctest slice green | no mutable `SelectScene` |
| R-TEX-004 | Optional Basis/Draco/meshopt import-depth decoders | §11 | todo |  |  |  |  | Phase 11b/stretch; not Phase 12 blocker |

## Validation (every change)

| ID | Item | Source | Status | Proof / link | Notes |
|---|---|---|---|---|---|
| R-VAL-001 | `check_runtime_completeness.sh` green | Validation | todo |  |  |
| R-VAL-002 | Build + `-L graphics3d` green mac/win/linux | Validation | todo |  | CO-1 |
| R-VAL-003 | `lint_platform_policy.sh` + `run_cross_platform_smoke.sh` | Validation | done | `../../3dnextlevel3/policy-audit-closure.md`; `./scripts/lint_platform_policy.sh --strict --changed-only`; `./scripts/run_cross_platform_smoke.sh --build-dir cmake-build-debug` | Smoke script now capability-gates disabled-surface tests and skips missing executables in partial build trees; final local lane passed after building linker smoke targets |
| R-VAL-004 | Before/after perf number per scale feature | Validation | todo |  | GATE-004 |
| R-VAL-005 | `runFrames` VM/native determinism per sim change | Validation | done | `../../3dnextlevel3/determinism-closure.md`; focused local lane passed: `g3d_3dnext2_surface_probe`, `test_rt_game3d`, `test_codegen_env_is_native`, native-run Zia promise tests, and `test_crosslayer_arith` | Future simulation-touching changes must rerun the same gate; IL/VM/native semantic changes also need GATE-009 ADR evidence |
| R-VAL-006 | Software path + capability-gated GPU per visual feature | Validation | done | `../../3dnextlevel3/software-baseline-closure.md`; focused local gate passed 9/9 | GATE-005 closed locally; unsupported GPU features still require clean capability skips |
| R-VAL-007 | Success + negative/capability ctest per new fn, or waiver | Validation | todo |  |  |
