module HttpReqDemo;
bind Viper.Terminal;

func start() {
    Say("=== API Audit: Viper.Network.HttpReq ===");
    Say("NOTE: HttpReq requires network access to Send. API surface test only.");
    Say("Methods: New(method,url), SetHeader(key,val), SetBody(bytes), SetBodyStr(str), SetTimeout(ms), Send() -> HttpRes");
    Say("=== HttpReq Audit Complete (API surface only) ===");
}
