module HelloLSP;

// Try hovering over "start", or typing "Zanna." to get completions.
// Errors should appear as red squiggly underlines.

struct Point {
    expose Integer x;
    expose Integer y;
}

func start() {
    var greeting = "Hello from the Zia Language Server!";
    Zanna.Terminal.Say(greeting);

    var p: Point = new Point();
    p.x = 10;
    p.y = 20;
    Zanna.Terminal.SayInt(p.x);
    Zanna.Terminal.SayInt(p.y);
}
